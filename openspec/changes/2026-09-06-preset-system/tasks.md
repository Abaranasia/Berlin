# Tasks: Preset System (roadmap Phase 11)

Artifact store: hybrid. Delivery: **single-pr** (design's Migration/Rollout: single-commit revert deletes `Source/preset/`, the `PRESETS` row, and the new specs).

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~650-800 authored (`Preset.h` ~20, `PresetManager.h/.cpp` ~220, `MainComponent.h/.cpp` ~180, `PresetSerializationTests.cpp` ~220, `PresetManagerFileTests.cpp` ~110) |
| 400-line budget risk | High vs. the skill's default 400-line guard; within this session's orchestrator-authorized 800-line budget |
| Chained PRs recommended | No (single-pr strategy in effect; session budget is 800) |
| Suggested split | Single PR — phases below are internal commit checkpoints, not separate PRs |
| Delivery strategy | single-pr |
| Chain strategy | size-exception |

Decision needed before apply: Yes
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | `Preset`/`PresetResult` + pure `toValueTree`/`fromValueTree` core (no I/O) | PR 1 (single PR, phase checkpoint) | `BerlinTests.exe --category=Berlin --filter="*PresetSerialization*"` | N/A — pure unit-tested logic, no audio device needed | Revert `Source/preset/Preset.h` + the pure-core half of `PresetManager.h/.cpp`; delete `PresetSerializationTests.cpp`; zero callers exist yet |
| 2 | `PresetManager` file I/O (`fileForName`, `listPresetNames`, `save`, `load`) | PR 1 (same PR, phase checkpoint) | `BerlinTests.exe --category=Berlin --filter="*PresetManagerFile*"` | N/A — temp-directory-scoped unit tests, no real app-data writes | Revert the I/O half of `PresetManager.h/.cpp`; delete `PresetManagerFileTests.cpp`; Unit 1's pure core is unaffected |
| 3 | `MainComponent` wiring: `PresetManager` member, 5 widgets, `PRESETS` row, save/load/overwrite-confirm flow | PR 1 (same PR, final checkpoint) | `BerlinTests.exe --category=Berlin` (full suite) | Manual: launch Standalone, exercise save/load/overwrite-cancel/mid-playback-load per Phase 4 below | Revert `MainComponent.h/.cpp`; Units 1-2 remain inert (no dead code — `PresetManager` simply gains no caller) |

## Phase 1: `Preset`/`PresetResult` + `PresetManager`'s pure serialization core (preset-persistence delta)

- [x] 1.1 RED: create `Tests/Source/PresetSerializationTests.cpp` covering: round-trip of all 11 live fields + seed through `toValueTree → toXmlString → parseXML → fromValueTree`, asserted with `==` (not epsilon); the 8 effects fields equal `kDefaultPatch` after `fromValueTree` (Decision 5's structural-exclusion guarantee, since the serializer never writes them); the precision-regression case `resonance = 0.7071068f` round-trips bit-exact (the direct guard against `var::toString()`'s 6-significant-digit trap, per design's Verified Findings table); seed fidelity at `INT64_MIN`, `INT64_MAX`, `0`, `-1` — the case that rules JSON out; each of the 9 continuous parameters' `kMin*`/`kMax*` range endpoints round-trip bit-exact; `schemaVersion=2` (newer than `kSchemaVersion=1`) → `PresetResult::unsupportedVersion`, `out` left untouched; missing/garbage `schemaVersion` attribute → `PresetResult::parseFailed`, `out` untouched; wrong root tag, missing `<Synth>`, missing `<Generation>`, missing a required attribute → `parseFailed`/structural rejection, `out` untouched (Decision 4's reject branch); unrecognized `waveform`/`lfoDestination` enum name → rejected, `out` untouched (Decision 4's "no defensible nearest valid answer" case); an out-of-range continuous value (e.g. `cutoffHz="99999"`) → loads successfully with the value clamped to `kMinCutoffHz`/`kMaxCutoffHz` via the existing `clampParameter` (Decision 4's clamp branch, NOT a rejection). Spec: `preset-persistence` "Persisted Format Is Human-Readable And Version-Tagged", "Malformed Preset Files Are Handled Safely...", "Preset Scope Is Exactly The 11 Live Parameters Plus Seed".
- [x] 1.2 Register `Tests/Source/PresetSerializationTests.cpp`'s `<FILE>` entry in `Tests/BerlinTests.jucer` (build fails to compile it otherwise).
- [x] 1.3 GREEN: create `Source/preset/Preset.h` per design's Interfaces block — `Preset` aggregate (`juce::String name`, `SynthPatch patch`, `juce::int64 seed = 0`) and `enum class PresetResult { ok, nameInvalid, directoryUnavailable, writeFailed, fileNotFound, parseFailed, unsupportedVersion }`. Header-only, JUCE-aware.
- [x] 1.4 GREEN: create `Source/preset/PresetManager.h`/`.cpp` with `static constexpr int kSchemaVersion = 1;` and the pure static pair `toValueTree (const Preset&)` / `fromValueTree (const juce::ValueTree&, Preset& out)`. Implement Decision 1's binding serialization rule exactly: every property written as an explicitly formatted `juce::String` — floats via `juce::String (v, 9)`, seed via `juce::String (juce::int64)`, enums via a name table (`saw`/`square`/`pulse`/`triangle`; `pitch`/`cutoff`/`amplitude`/`pulseWidth`) — never a native `double`/`int64` var placed directly in the tree. Implement Decision 3's schema (`schemaVersion` root attribute, `<Synth>`/`<Generation>` child sections) and Decision 4's split policy (structural defects → `parseFailed`/rejection with `out` untouched; continuous out-of-range values → `clampParameter` then load ok; unknown enum name → reject).
- [x] 1.5 Register `Source/preset/Preset.h` and the pure-core declarations of `Source/preset/PresetManager.h`/`.cpp` `<FILE>` entries in `Berlin.jucer`.
- [x] 1.6 Verify: `BerlinTests.exe --category=Berlin --filter="*PresetSerialization*"` green.

## Phase 2: `PresetManager` file I/O (preset-persistence delta, Decision 2)

- [x] 2.1 RED: create `Tests/Source/PresetManagerFileTests.cpp` against a `File::createTempFile`-scoped temporary directory (cleaned up in the test, never the real app-data location), covering: `fileForName` on `../../evil`, `a/b`, `..`, `""`, whitespace-only, and a name of illegal-characters-only → either `juce::File()` or a direct child of the preset directory, never outside it (Threat Matrix's containment row — `result.getParentDirectory() == presetDirectory`); save → `listPresetNames()` contains the saved name → `load` returns a `Preset` equal to what was saved; saving under the same name twice overwrites the file in place (no duplicate/stale copy); an unparseable file placed directly in the directory is silently omitted from `listPresetNames()`, not fatal; enumeration of a missing/absent directory returns an empty list without error, and the directory is auto-created on first `save`. Spec: `preset-persistence` "Browse Available Presets", plus the Threat Matrix's path-construction row.
- [x] 2.2 Register `Tests/Source/PresetManagerFileTests.cpp`'s `<FILE>` entry in `Tests/BerlinTests.jucer`.
- [x] 2.3 GREEN: implement `PresetManager::PresetManager (juce::File presetDirectory = defaultPresetDirectory())`, `static juce::File defaultPresetDirectory()` (`userApplicationDataDirectory/Berlin/Presets`), `fileForName` (sanitize via `createLegalFileName`, then assert containment per the Threat Matrix — return `juce::File()` on failure), `listPresetNames()` (`findChildFiles (File::findFiles, false, "*.xml")`, parse each to read its `name` property, sorted, unparseable files silently excluded), `save` (unconditional; caller has already confirmed any overwrite; `createDirectory()`-guarded per Decision 2's consequence (c)), `load` (`out` left untouched unless the result is `ok`).
- [x] 2.4 Verify: `BerlinTests.exe --category=Berlin --filter="*PresetManagerFile*"` green. Confirm no test wrote to the real `userApplicationDataDirectory`.

## Phase 3: `MainComponent` wiring (preset-persistence + generation-live-control deltas) — highest-risk phase

- [x] 3.1 `Source/MainComponent.h` — add `PresetManager presetManager;` member; add 5 widgets per Decision 6's row: `juce::Label presetSectionLabel;` (leading `PRESETS` label per the row layout), `juce::TextEditor presetNameEditor;`, `juce::TextButton savePresetButton { "Save" };`, `juce::ComboBox presetBox;`, `juce::TextButton loadPresetButton { "Load" };`; declare `SynthPatch currentPatchFromWidgets() const;`, `void applyPatchToWidgets (const SynthPatch&);`, `void savePreset();`, `void loadSelectedPreset();`, `void refreshPresetList();`, `juce::String describePresetFailure (berlin::PresetResult) const;`. Do NOT touch the declarations of `regenerate()` or `pushAllParametersToSynth()` (Decision 5 — reused verbatim).
- [x] 3.2 `Source/MainComponent.cpp` constructor — add a `// ---- Preset controls (roadmap Phase 11 / preset-system) ----` block: `presetSectionLabel` text `"PRESETS"`; `presetNameEditor` empty by default; `savePresetButton.onClick = [this] { savePreset(); }`, disabled while the trimmed name editor text is empty (Decision 6: "Save disabled while the name sanitizes to empty" — wire an editor-text listener or check at click time per whichever the codebase's existing `onTextChange` idiom supports); `presetBox` populated via `refreshPresetList()` called once at the end of the constructor; `loadPresetButton.onClick = [this] { loadSelectedPreset(); }`, disabled while nothing is selected in `presetBox` (Decision 6: "Load disabled while nothing is selected" — selecting in the ComboBox never auto-loads).
- [x] 3.3 GREEN: implement `MainComponent::currentPatchFromWidgets()` — read the same 11 widgets `pushAllParametersToSynth()` reads (`waveformBox`, `cutoffSlider`, `resonanceSlider`, `pulseWidthSlider`, `attackSlider`, `decaySlider`, `sustainSlider`, `releaseSlider`, `lfoRateSlider`, `lfoDepthSlider`, `lfoDestinationBox`) into a fresh `SynthPatch` (the 8 effects fields stay `kDefaultPatch` by default member initialization, per Decision 5's structural-exclusion note); implement `MainComponent::applyPatchToWidgets (const SynthPatch&)` — set each of the same 11 widgets with `juce::dontSendNotification` (so restoring widgets does not redundantly fire the 11 `onValueChange`/`onChange` callbacks mid-update, per Decision 5).
- [x] 3.4 GREEN: implement `MainComponent::savePreset()` per the Data Flow's SAVE path: trim `presetNameEditor`'s text; empty → `statusLabel` reports why, stop; build `Preset { name, currentPatchFromWidgets(), currentSeed }`; `file = presetManager.fileForName (name)`; if `file.existsAsFile()`, guard re-entrancy with `saveButton.setEnabled (false)` (design's exact wording — apply to `savePresetButton`) and launch `juce::NativeMessageBox::showOkCancelBox (QuestionIcon, "Overwrite Preset?", ..., this, ModalCallbackFunction::create (...))` guarded by a `juce::Component::SafePointer<MainComponent>` (Decision 7 — required because, unlike `exportChooser`, a message box is not an owned member reset in the destructor); on OK (or no existing file), call `presetManager.save (preset)` then `refreshPresetList()` selecting the newly saved name; on Cancel, leave the file untouched and re-enable the button.
- [x] 3.5 GREEN: implement `MainComponent::loadSelectedPreset()` per the Data Flow's LOAD path: `presetManager.load (presetBox.getText(), preset)`; on failure, `statusLabel` reports `describePresetFailure (result)` in red, synth and sequence left untouched (do NOT call `applyPatchToWidgets`/`pushAllParametersToSynth`/`regenerate` on failure); on success, `applyPatchToWidgets (preset.patch)` (dontSendNotification), call the EXISTING unchanged `pushAllParametersToSynth()`, set `currentSeed = preset.seed` and `seedEditor.setText (juce::String (currentSeed), dontSendNotification)`, populate `presetNameEditor` with the loaded name (so re-saving naturally targets the same preset, per Decision 6), then call the EXISTING unchanged `regenerate (false)` (Decision 5 — `drawNewSeed=false` means `regenerate`'s Lock-Seed branch is never consulted, so the saved seed applies even if Lock Seed is on, satisfying `generation-live-control`'s "Loading Applies Its Saved Seed, Then Generates"). Do NOT modify `regenerate()` or `pushAllParametersToSynth()` themselves.
- [x] 3.6 GREEN: implement `MainComponent::refreshPresetList()` (repopulate `presetBox` from `presetManager.listPresetNames()`, optionally re-selecting a given name) and `MainComponent::describePresetFailure (berlin::PresetResult)` mirroring the existing `describeWriteFailure` pattern (`MainComponent.cpp:22-36`) — one message per `PresetResult` enumerator.
- [x] 3.7 `Source/MainComponent.cpp::resized()` — add the `PRESETS` row per Decision 6's exact placement (after `generationButtonRow`, before the two-column split) and exact widths: `presetLabel 96 │ presetNameEditor 180 │ 6 │ Save 140 │ 6 │ presetBox 180 │ 6 │ Load 140` (≤ 776), one shared 34px row (`kControlHeight + kMargin/2`). No change to `setSize (800, 600)`; residual right-column slack becomes 44px per design's re-derivation.
- [x] 3.8 Register `Source/preset/Preset.h` and `Source/preset/PresetManager.h`/`.cpp`'s full (I/O-included) `<FILE>` entries in `Berlin.jucer` if not already complete from Phase 1.5/2; confirm `Tests/BerlinTests.jucer` has both new test files registered from Phases 1.2/2.2.
- [x] 3.9 Build `Berlin.sln`; confirm clean compile.

## Phase 4: Manual audibility/correctness gate (human-only, not automatable)

BLOCKED on a human tester with an audio device - out of scope for this sdd-apply run per its explicit instructions. All of Phase 1-3 and Phase 5's non-gated checks are complete; 4.1-4.5 below remain open.

- [ ] 4.1 Set the 11 widgets and a specific seed, save under a new name, quit the app, relaunch, load that preset — confirm all 11 widgets AND the seed are restored, and the audio matches what was set before quitting.
- [ ] 4.2 While audio is playing partway through the sequence, load a different preset — confirm playback restarts from step 1 with no hung note, dropout, assert, or crash.
- [ ] 4.3 Save under an existing name, get the overwrite prompt, click Cancel — confirm the existing file on disk is byte-identical to before (no partial write).
- [ ] 4.4 Enable Lock Seed, then Load a preset with a different saved seed — confirm the seed field and audio change to the preset's saved seed anyway (Lock Seed does not suppress explicit load).
- [ ] 4.5 Open a saved `*.xml` preset file in a text editor — confirm its structure and values are human-readable (Decision 1's human-readability goal).

## Phase 5: Final cleanup & verification

- [x] 5.1 Run the full suite: `BerlinTests.exe --category=Berlin` exits 0, including the new `PresetSerializationTests`/`PresetManagerFileTests` scenarios. (163/163 scenarios green, exit 0.)
- [x] 5.2 RT-safety review (juce-app-dev): confirm ZERO changes to `Source/synth/SynthEngine.*`, `SynthVoice.*`, `SynthEffects.*`, `Source/playback/SequencePlayer.*`, `Source/playback/Transport.*` — this is the first 100%-message-thread phase in this project's history (design's own claim); verify via `git diff --stat` against the pre-change base commit that all listed paths show zero diff. Confirm no allocation/lock/log was added to any audio-thread path (none should exist — this phase touches only `Source/preset/*` and `Source/MainComponent.*`, both message-thread-only). (Confirmed: `git diff --stat ef9c130 -- <listed paths>` is empty.)
- [x] 5.3 Confirm byte-for-byte no diff on all other out-of-scope tiers: `Source/core/*`, `Source/generation/*`, `Source/midi/*`, `Source/export/*`, `Source/synth/SynthPatch.h` (reused unmodified per Decision 5). (Confirmed: `git diff --stat ef9c130 -- <listed paths>` is empty.)
- [x] 5.4 Spec merge: apply the three spec deltas into their base specs per `openspec-convention.md` — new `preset-persistence` spec created at `openspec/specs/preset-persistence/spec.md`; `internal-synth-voice`'s Purpose text corrected (no Requirement changes); `generation-live-control`'s "Lock Seed Suppresses Reseeding" requirement modified in place plus its new "Loading A Preset Applies Its Saved Seed, Then Generates" requirement added.
- [x] 5.5 Confirm the reconciled spec-delta content noted by the orchestrator (ValueTree+XML+explicit-String format, split reject/clamp policy, schema-versioning clause) matches what actually shipped in Phase 1-2's implementation — no drift between spec and code. (Confirmed: PresetManager.cpp uses juce::String(v,9)/String(int64) exclusively, fromValueTree rejects structural defects and clamps continuous out-of-range values, kSchemaVersion/unsupportedVersion/parseFailed gate matches Decision 3 exactly.)

## Rules Applied

- Strict TDD: Phases 1-2 are RED-before-GREEN (`PresetSerializationTests`, `PresetManagerFileTests`).
- `regenerate()` and `pushAllParametersToSynth()` are NOT modified — Decision 5's entire point is that the load path needs no new synth plumbing; only `MainComponent` newly *calls* them in sequence after restoring widgets.
- `SynthPatch.h` is reused unmodified — stays JUCE-free per its documented convention exception; only 11 of its 19 fields participate in a preset.
- `Berlin.jucer`/`Tests/BerlinTests.jucer` DO need `<FILE>` registration updates for every new file (`Preset.h`, `PresetManager.h/.cpp`, `PresetSerializationTests.cpp`, `PresetManagerFileTests.cpp`) — a miss fails loudly as an unresolved external, consistent with Phase 10's note ("unlike Phase 9").
- Preset directory root is `userApplicationDataDirectory/Berlin/Presets` per design's Open Questions — flagged as reversible in one line but not blocking; do not switch to `userDocumentsDirectory` without a design amendment.
