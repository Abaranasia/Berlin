# Tasks: Internal Synth (roadmap Phase 8)

Artifact store: hybrid. File: `openspec/changes/internal-synth/tasks.md`. Delivery: **chained-pr**, 4 slices, per `design.md`'s Migration/Rollout section and the proposal's own recommended order (confirmed here, not reopened).

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~900-1300 authored (excl. generated `Builds/`/`JuceLibraryCode/`/`Tests/Builds/`/`Tests/JuceLibraryCode/`) — new `Source/synth/` tier (~400-500), new `Tests/Source/` synth suites (~350-500), `MainComponent.h/.cpp` wiring (~60-90), two `.jucer` module/file registrations (~30-50 authored XML lines; generated regen churn excluded from the count but real and reviewer-visible) |
| 400-line budget risk | High (per proposal's Delivery forecast) |
| Chained PRs recommended | Yes — confirmed |
| Suggested split | PR1 module/build plumbing + link smoke → PR2 `SynthVoice` DSP (oscillator+filter+ADSR), unit-tested, unwired → PR3 `SynthEngine` + `MainComponent` wiring + synth enable toggle, first audible sound → PR4 `Lfo` + four destinations + `SynthEffects` (delay/reverb) + FX toggle |
| Delivery strategy | chained-pr |
| Chain strategy | feature-branch-chain |

Decision needed before apply: **No** — the four-slice split is confirmed here at `sdd-tasks`, exactly matching `design.md`'s Migration/Rollout section and the proposal's Delivery forecast. PR1 is a hard blocker for PR2-4: `Source/synth/` cannot compile until `juce_dsp` is registered in `Berlin.jucer` and `Tests/BerlinTests.jucer`.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|--------------------|
| 1 | `juce_dsp` in `Berlin.jucer`; `juce_dsp`+`juce_audio_formats`+`juce_audio_basics` in `Tests/BerlinTests.jucer`; both regens; `DspLinkSmokeTests.cpp` | PR 1 (base: `main`) | `BerlinTests.exe --category=Berlin --name=DspLinkSmoke` | Console run, check exit code | Restore both `.jucer` files to `main` and re-run `Projucer.exe --resave` on each; no source-tier files exist yet |
| 2 | `Source/synth/SynthPatch.h`, `Source/synth/SynthVoice.h/.cpp` (oscillator + filter + ADSR), unwired, fully unit-tested | PR 2 (base: PR1 branch) | `BerlinTests.exe --category=Berlin --name=SynthVoice` | Console run, check exit code | Delete `Source/synth/SynthPatch.h`, `SynthVoice.h/.cpp`, `Tests/Source/SynthVoiceTests.cpp`; revert the `synth` group `<FILE>` entries in both `.jucer` files; `MainComponent` untouched |
| 3 | `Source/synth/SynthEngine.h/.cpp` + `MainComponent` wiring + synth enable/disable toggle | PR 3 (base: PR2 branch) | `BerlinTests.exe --category=Berlin --name=SynthEngine` | Console run (automated) + manual: launch `Berlin.exe`, confirm audible sound with no MIDI device attached, confirm toggle silences it | Revert `Source/MainComponent.h/.cpp` to PR2's state (byte-verifiable); delete `SynthEngine.h/.cpp` + `SynthEngineTests.cpp`; revert their `<FILE>` entries |
| 4 | `Source/synth/Lfo.h/.cpp` + four destinations, `Source/synth/SynthEffects.h/.cpp` (delay+reverb), FX bypass toggle | PR 4 (base: PR3 branch) | `BerlinTests.exe --category=Berlin` (full, including `Lfo`/extended `SynthEngine` suites) | Console run (automated) + Phase 5 manual audibility gate | Revert `Source/MainComponent.h/.cpp` to PR3's state; delete `Lfo.h/.cpp`, `SynthEffects.h/.cpp`, `LfoTests.cpp`; revert extended `SynthEngine` render/reset changes to PR3's version |

Confirm at implementation start: synth test suites register under the `Berlin` category (matching all 17 existing suites) unless a real `Projucer` constraint forces otherwise — if so, document the actual category name used and update the focused test commands above and the final gate command in Phase 6.

## Phase 1: Module/Build Plumbing + Link Smoke Test (PR #1 — hard blocker for everything else)

- [x] 1.1 In `Berlin.jucer`, add `<MODULE id="juce_dsp" showAllCode="1" useLocalCopy="0" useGlobalPath="1"/>` to `<MODULES>`, landing alphabetically between `juce_data_structures` and `juce_events` (line ~57-58 today).
- [x] 1.2 In `Berlin.jucer`, add the sibling `<MODULEPATH id="juce_dsp" path="../../Projucer/modules"/>` to `<MODULEPATHS>` in the same alphabetical position as its `<MODULE>` row.
- [x] 1.3 In `Tests/BerlinTests.jucer`, add three `<MODULE>` rows — `juce_audio_basics`, `juce_audio_formats`, `juce_dsp` — each `showAllCode="1" useLocalCopy="0" useGlobalPath="1"`, landing alphabetically around the existing `juce_core` row (final order: `juce_audio_basics`, `juce_audio_formats`, `juce_core`, `juce_dsp`). `juce_audio_formats` is non-optional — `juce_dsp` declares it as a dependency (design's verified-corrections table).
- [x] 1.4 In `Tests/BerlinTests.jucer`, add the three matching `<MODULEPATH id="..." path="../../../Projucer/modules"/>` rows to `<MODULEPATHS>`, same alphabetical order as 1.3.
- [x] 1.5 Run `Projucer.exe --resave` on `Berlin.jucer`; confirm it emits `JuceLibraryCode/include_juce_dsp.cpp`/`.mm`, rewrites `JuceLibraryCode/JuceHeader.h`, and rewrites `Builds/VisualStudio2026/Berlin.vcxproj` + `.vcxproj.filters`. Exit 0.
- [x] 1.6 Run `Projucer.exe --resave` on `Tests/BerlinTests.jucer`; confirm `Tests/JuceLibraryCode/` grows from 6 files to roughly a dozen (including `include_juce_audio_formats_flac_1.c`/`_flac_2.c`, per design's module-mechanics note). Exit 0.
- [x] 1.7 Diff both `.jucer` files' `<MODULES>`/`<MODULEPATHS>` sections; confirm `useGlobalPath="1"` and (for the test project) `useAppConfig="0"` are preserved on every new row, matching the existing `juce_core` row's shape.
- [x] 1.8 Create `Tests/Source/DspLinkSmokeTests.cpp`: construct + `prepare` a `juce::dsp::Oscillator<float>`, assert `processSample(0.0f)` returns a finite value. This is the only file that may depend on the new modules in this slice (per Testing Strategy's Build row).
- [x] 1.9 Register `DspLinkSmokeTests.cpp` as a `<FILE>` in `Tests/BerlinTests.jucer`'s `Source` group; re-resave; confirm no other `<MODULES>`/`<MODULEPATH>` diff beyond 1.3/1.4.
- [x] 1.10 Build `Berlin.sln` (GUI app target): confirm clean compile with the new `juce_dsp` module present but unreferenced by any app source file yet.
- [x] 1.11 Build `Tests/BerlinTests.sln`: confirm clean compile and link with the three new modules.
- [x] 1.12 Run `BerlinTests.exe --category=Berlin`; confirm exit 0, all 17 pre-existing suites still green (regression), plus the new `DspLinkSmokeTests` suite passing.
- [x] 1.13 Confirm no `Source/synth/` files exist yet and `Source/MainComponent.h/.cpp` are byte-for-byte unchanged — PR #1 is generated churn plus one smoke test only, reviewable without any DSP reasoning (per design's isolation rationale).

## Phase 2: Oscillator + ADSR + Filter DSP (PR #2 — unwired, unit-tested)

- [x] 2.1 Create `Source/synth/SynthPatch.h`: JUCE-free POD — waveform enum (saw/square/pulse/triangle), cutoff/resonance, ADSR attack/decay/sustain/release times, LFO rate/depth/destination enum (pitch/cutoff/amplitude/pulseWidth), delay/reverb params, output level; one `constexpr` default patch. Confirm zero `#include <juce_...>` (scoped-exception guardrail (a)).
- [x] 2.2 Create `Source/synth/SynthVoice.h`: declare the class holding `juce::dsp::Oscillator<float>`, `juce::dsp::StateVariableTPTFilter<float>`, `juce::ADSR`; mark it `JUCE_DECLARE_NON_COPYABLE` (Decision 3 — the pulse generator lambda captures `this`); declare `prepare(const juce::dsp::ProcessSpec&)`, `noteOn(float frequencyHz)`, `noteOff()`, `render(float* left, float* right, int numSamples)` (or equivalent per-block render signature), `reset()`, and whatever silence/idle query the unit tests below need. *(internal-synth-voice: all requirements)*
- [x] 2.3 Implement the oscillator's four waveform generators in `SynthVoice.cpp` (saw/square/triangle as ordinary `initialise(fn, n)` lookup-table generators; pulse per Decision 3's exact live-read lambda with `lookupTableNumPoints = 0`, reading a live `pulseWidth` member). Oscillator is additive (`processSample` returns `input + generator(...)`) — pass `0.0f` as the raw input. *(internal-synth-voice: Four Selectable Oscillator Waveforms)*
- [x] 2.4 Implement the low-pass filter stage: `StateVariableTPTFilter` applied after the oscillator, with configurable cutoff and resonance from the patch. *(internal-synth-voice: Low-Pass Filter With Resonance)*
- [x] 2.5 Implement ADSR-gated amplitude: `noteOn()` triggers attack (does not zero any in-flight envelope value, so a retrigger ramps from wherever release left off — Decision 7's no-click property); `noteOff()` triggers release; post-filter signal is multiplied by `adsr.getNextSample()`. *(internal-synth-voice: ADSR-Gated Amplitude)*
- [x] 2.6 Implement silence-when-idle: a freshly prepared, never-triggered voice renders exact zero samples. *(internal-synth-voice: Silence When Idle)*
- [x] 2.7 Implement `prepare()` performing every allocating call for this voice — `Oscillator::initialise` + `prepare`, `StateVariableTPTFilter::prepare`, `ADSR::setSampleRate` + `setParameters` — and confirm `render()`/`noteOn()`/`noteOff()` call none of them (Decision 5). *(internal-synth-voice: Allocation-Free Voice Rendering)*
- [x] 2.8 Create `Tests/Source/SynthVoiceTests.cpp` per the Testing Strategy table: silent while idle; non-silent after `noteOn`; envelope non-decreasing through attack; returns to exact silence after release completes; output bounded (gain-staging guard). Cover each of the four waveforms producing a periodic, distinguishable signal, and the filter cutoff/resonance sweep behavior, at the level testable offline without the LFO (LFO lands in Phase 4).
- [x] 2.9 Register `SynthPatch.h`, `SynthVoice.h/.cpp`, `SynthVoiceTests.cpp` — a new `synth` `<GROUP>` — in both `Berlin.jucer` (app: `SynthPatch.h`, `SynthVoice.h/.cpp` only) and `Tests/BerlinTests.jucer` (relative-path registration of the same `SynthPatch.h`/`SynthVoice.h/.cpp`, plus `SynthVoiceTests.cpp` in the `Source` group) — no duplication, per `unit-test-harness`'s shared-file convention.
- [x] 2.10 Re-resave both `.jucer` files; confirm zero `<MODULES>`/`<MODULEPATH>` diff (module set was already finalized in Phase 1).
- [x] 2.11 Build `Berlin.sln` and `Tests/BerlinTests.sln`; run `BerlinTests.exe --category=Berlin`; confirm exit 0, new `SynthVoice` suite green, no regression.
- [x] 2.12 Confirm `Source/MainComponent.h/.cpp` are byte-for-byte unchanged from Phase 1's end state — the app still renders silence (unwired).
- [x] 2.13 **Code-review gate (explicit, not just an implementation task)**: confirm `SynthVoice` is genuinely non-copyable and non-movable — the pulse-width generator lambda captures `this`, so a copy or move would leave a dangling generator inside the moved-from/copied instance. Verify `JUCE_DECLARE_NON_COPYABLE` (or equivalent) is present and that no code path elsewhere (containers, `std::optional`, return-by-value) attempts to copy or move a `SynthVoice`.

## Phase 3: SynthEngine + MainComponent Wiring — First Audible Sound (PR #3 — valid intermediate merge point)

- [x] 3.1 Create `Source/synth/SynthEngine.h`: owns exactly one `SynthVoice` member by value (Decision 1 — no pool, no stealing); `juce::AudioBuffer<float> scratch` sized `(2, samplesPerBlockExpected)`; `std::atomic<bool> enabled { true }` plus a private audio-thread-only `wasEnabled` edge latch (Decision 6, synth-enable half only — `effectsEnabled` lands in Phase 4); declare `prepare(const juce::dsp::ProcessSpec&)`, `render(const StepEventBuffer&, juce::AudioBuffer<float>&, int startSample, int numSamples) noexcept`, `reset() noexcept`, `setEnabled(bool) noexcept` per the design's Interfaces block. *(internal-synth-output: Strictly Monophonic dependency, Allocation-Free Synth Contribution)*
- [x] 3.2 Implement `SynthEngine::prepare()`: `scratch.setSize(2, spec.maximumBlockSize)`, `voice.prepare(spec)` — every allocating call happens here (Decision 5).
- [x] 3.3 Implement `SynthEngine::render()`'s segment loop: split at event offsets, origin 0 inside `scratch` (Decision 4); for each event in buffer order, `isNoteOn` calls `voice.noteOn(juce::MidiMessage::getMidiNoteInHertz(event.note))`; `!isNoteOn` calls `voice.noteOff()` **only if** `event.note == currentNote` (Decision 7 — track `currentNote` in the engine or voice, whichever the implementation settles on, and keep it consistent); render each segment into `scratch` channels 0/1. *(internal-synth-output: StepEventBuffer Consumption, Sample-Offset-Accurate Note Rendering)*
- [x] 3.4 Implement the `enabled` gate inside `render()`: when `false`, skip voice rendering entirely; on the `true`→`false` edge (via `wasEnabled`), reset the voice exactly once so no envelope keeps sustaining (Decision 6). *(internal-synth-output: Synth Enable/Disable Toggle)*
- [x] 3.5 Implement the clamp: `render()` clamps `numSamples` to `scratch.getNumSamples()` with a `jassert`, never resizes (Decision 4).
- [x] 3.6 Implement the terminal `addFrom`: only this final step applies `bufferToFill`'s `startSample` — `addFrom(ch, startSample, scratch, ch, 0, numSamples)` — everywhere else works in `scratch`'s own origin-0 space (Decision 4).
- [x] 3.7 Implement `SynthEngine::reset()`: envelope idle, oscillator phase zero, filter state zero, scratch zeroed (delay/reverb reset is added in Phase 4, since those components don't exist yet).
- [x] 3.8 Implement `SynthEngine::setEnabled(bool)` writing the atomic from the message thread.
- [x] 3.9 Create `Tests/Source/SynthEngineTests.cpp` (Phase 3 scope): synthetic `StepEventBuffer` — first non-zero sample lands at `startSample + sampleOffset`; `setEnabled(false)` leaves `destination` bit-identical (silent); `reset()` silences a sounding voice; note-off with a mismatched note does not cut the voice; `numSamples` beyond the prepared block size is clamped, never resized.
- [x] 3.10 Register `SynthEngine.h/.cpp` + `SynthEngineTests.cpp` in both `.jucer` projects (app + tests, `synth` group); re-resave both; confirm zero `<MODULES>` diff.
- [x] 3.11 `Source/MainComponent.h`: add `#include "synth/SynthEngine.h"`; add `berlin::SynthEngine synth;` member after the existing domain members (line ~46, after `midiBlock`); add `juce::ToggleButton synthToggle { "Synth" };` (or equivalent label) after `exportButton`/`statusLabel`, per UI Pattern v1 (Decision 9).
- [x] 3.12 `Source/MainComponent.cpp::prepareToPlay`: delete the `juce::ignoreUnused (samplesPerBlockExpected);` line (line 103); build a `juce::dsp::ProcessSpec` from `sampleRate`/`samplesPerBlockExpected`/output channel count and call `synth.prepare(spec)`.
- [x] 3.13 `Source/MainComponent.cpp::getNextAudioBlock`: add one `synth.render (blockEvents, *bufferToFill.buffer, bufferToFill.startSample, bufferToFill.numSamples);` line, additive alongside the existing `player.process`/`midiTranslator.translate`/`midiSink.dispatch` lines (lines 111-114) — none of those three lines change. *(realtime-audio-wiring: Audio Output Governed By Synth Enable State)*
- [x] 3.14 `Source/MainComponent.cpp::releaseResources`: add `synth.reset();` alongside the existing `player.flushPendingNoteOff` → MIDI path (lines 125-129), which stays byte-unchanged (Decision 8).
- [x] 3.15 `Source/MainComponent.cpp` ctor: `addAndMakeVisible (synthToggle);`; `synthToggle.setToggleState (true, juce::dontSendNotification);` (or rely on the atomic's own default — keep the visible control and the atomic in sync); `synthToggle.onClick = [this] { synth.setEnabled (synthToggle.getToggleState()); };`; placed before `setSize (800, 600);`, which stays the last statement of the UI section (UI Pattern v1).
- [x] 3.16 `Source/MainComponent.cpp::resized()`: add a `synthToggle` row via `removeFromTop`/`removeFromLeft` off `getLocalBounds().reduced (kMargin)`, alongside the existing `exportButton`/`statusLabel` rows.
- [x] 3.17 Build `Berlin.sln`; confirm clean compile.
- [x] 3.18 Build `Tests/BerlinTests.sln`; run `BerlinTests.exe --category=Berlin`; confirm exit 0, `SynthEngine` suite green, no regression.
- [x] 3.19 **(human-verified 2026-09-05)** Developer smoke check (quick sanity, not the exhaustive Phase 5 gate): launch `Berlin.exe`, confirm audible sound with no MIDI device connected; toggle `synthToggle` off and confirm the output goes silent while the transport keeps running. Confirmed by user: sound audible, toggle silences/resumes correctly.
- [x] 3.20 Confirm `Source/midi/*`, `Source/export/*`, `Source/core/*`, `Source/generation/*`, `Source/playback/*` remain byte-for-byte unchanged after this slice.

## Phase 4: LFO + Four Destinations + SynthEffects (Delay/Reverb) + FX Toggle (PR #4)

- [x] 4.1 Create `Source/synth/Lfo.h`/`.cpp`: JUCE-free `double` phase accumulator — `prepare(sampleRate)`, `setRate(hz)`, `advance(int numSamples)`, `getValue()` returning `[-1, 1]` (Decision 2). Confirm zero `#include <juce_...>` (scoped-exception guardrail (a)).
- [x] 4.2 Create `Tests/Source/LfoTests.cpp`: exact period at a known rate; range stays in `[-1, 1]`; `advance(n)` equals `n` × `advance(1)` (Testing Strategy table).
- [x] 4.3 Wire the LFO into the render path at **control rate**: recompute modulation once per 32-sample control block, subdividing the existing event-boundary segments (Decision 2) — not per-sample.
- [x] 4.4 Wire LFO destination — pitch: `osc.setFrequency(f, true)` with `force=true` so the LFO does not fight the oscillator's 0.05s frequency ramp (verified-corrections table). *(internal-synth-voice: Single LFO With Selectable Destination)*
- [x] 4.5 Wire LFO destination — filter cutoff: `filter.setCutoffFrequency(...)` called only at the 32-sample control rate, never per-sample (avoids a `std::tan` per sample, per Decision 2's rationale).
- [x] 4.6 Wire LFO destination — amplitude: post-ADSR gain multiply by the LFO value.
- [x] 4.7 Wire LFO destination — pulse width: write the live `pulseWidth` member the pulse generator lambda reads (Decision 3).
- [x] 4.8 Create `Source/synth/SynthEffects.h`/`.cpp`: terminal `juce::dsp::DelayLine` + `juce::dsp::Reverb` pass over a stereo `juce::dsp::AudioBlock` view of `scratch`; `prepare()` performs `DelayLine::setMaximumDelayInSamples` + `prepare` and `Reverb::prepare` (Decision 5 — all allocating calls off the audio thread); `process()` applies both. *(internal-synth-output: Terminal Delay And Reverb, Bypassed By Default)*
- [x] 4.9 Extend `SynthEngine`: add `std::atomic<bool> effectsEnabled { false }` plus its own private `wasEffectsEnabled` edge latch (Decision 6); own a `SynthEffects` member; `render()`'s terminal stage becomes `effectsEnabled ? effects.process(scratchBlock) : (dry, unchanged)`. Reject `dsp::Reverb::setEnabled` as the mechanism — it defaults to `true` and would silently contradict bypassed-by-default (Decision 6's stated rationale).
- [x] 4.10 On the `effectsEnabled` `true`→`false` edge, hard-clear the delay/reverb tail exactly once so no new processed tail is introduced and no click/glitch is audible at the transition. *(internal-synth-output: Terminal Delay And Reverb — "Enabling then disabling stops new tail without artefacts")*
- [x] 4.11 Extend `SynthEngine::reset()` to also clear the delay/reverb tail, completing Decision 8's full hard-reset (deferred from Phase 3 since these components didn't exist yet). *(internal-synth-output: No Stuck Notes On Stop Or Device Restart)*
- [x] 4.12 Implement `SynthEngine::setEffectsEnabled(bool) noexcept` writing the atomic from the message thread.
- [x] 4.13 Extend `Tests/Source/SynthEngineTests.cpp`: `effectsEnabled=false` (default) produces bit-identical dry output vs. the voice's own render; enabling then disabling introduces no new tail after the edge; `reset()` clears an active effect tail; a sustained full-level note with effects enabled never exceeds the valid output range (headroom/no-clip guard). *(internal-synth-output: Mixing Without Clipping)*
- [x] 4.14 `Source/MainComponent.h`: add `juce::ToggleButton fxToggle { "FX" };` (or equivalent label) alongside `synthToggle`, per UI Pattern v1.
- [x] 4.15 `Source/MainComponent.cpp` ctor: `addAndMakeVisible (fxToggle);`; `fxToggle.onClick = [this] { synth.setEffectsEnabled (fxToggle.getToggleState()); };`; default off, consistent with the atomic's `false` default (do not force `setToggleState(true, ...)`).
- [x] 4.16 `Source/MainComponent.cpp::resized()`: add an `fxToggle` row.
- [x] 4.17 Register `Lfo.h/.cpp`, `SynthEffects.h/.cpp`, `LfoTests.cpp` in both `.jucer` projects (`Lfo` is JUCE-free but lives in `synth`; `SynthEffects` depends only on `juce_dsp`, both already in the harness's module set); re-resave both; confirm zero `<MODULES>` diff (module set was finalized in Phase 1).
- [x] 4.18 Build `Berlin.sln` and `Tests/BerlinTests.sln`; run `BerlinTests.exe --category=Berlin`; confirm exit 0, `Lfo` suite and extended `SynthEngine` suite green, no regression.
- [x] 4.19 Confirm `Source/midi/*`, `Source/export/*` remain byte-for-byte unchanged after this slice (full final confirmation repeated in Phase 6).

## Phase 5: Manual Audibility Gate (human-only, not automatable)

- [x] 5.1 **(human-verified 2026-09-06)** Launch the built standalone app with no MIDI device connected and confirm, in order: (a) sound is audible from the seeded sequence by default; (b) each of the four waveforms is audibly distinct when exercised via the fixed patch; (c) the filter cutoff + resonance sweep audibly brightens/emphasizes with no dropout; (d) all four ADSR stages (attack, decay, sustain, release) are perceivable across note-on/note-off; (e) each of the four LFO destinations (pitch, filter cutoff, amplitude, pulse width) produces an audibly distinct modulation; (f) delay and reverb are each audible when enabled and cleanly bypassable to a dry signal with no click/glitch at the transition; (g) no drone or hanging note is audible after stopping playback or restarting the audio device; (h) disabling the synth toggle removes the doubled sequence when an external MIDI-out synth (Phase 5's roadmap precedent: Phase 5 `midi-output-routing`) is also active.

  All eight checks confirmed by the user via rebuild-and-listen cycles (temporarily editing `kDefaultPatch` per check, then reverted to canonical defaults — waveform=saw, cutoffHz=4000, resonance=0.7071068, pulseWidth=0.5, lfoDepth=0.0, lfoDestination=pitch): (a) audible by default; (b) saw/square/pulse(0.25 duty)/triangle all distinct; (c) 300Hz vs 9000Hz cutoff audibly brighter, no dropout; 1200Hz+resonance=9.0 produced an audible resonant peak, no instability; (d) already perceivable via default ADSR through normal note on/off; (e) pitch=vibrato, cutoff=wah, amplitude=tremolo, pulseWidth(with waveform=pulse)=timbral PWM shift, all confirmed distinct; (f) FX toggle cleanly bypasses delay/reverb with no click; (g) stopping playback/restarting audio device leaves no stuck note.

  **(h) discovery**: `MainComponent.cpp:85`'s `midiSink.openFirstAvailableDevice()` auto-opens the Windows "Microsoft GS Wavetable Synth" on launch, which was already playing the sequence as a "piano" GM patch alongside the internal synth — this doubled-playback scenario was live the whole session, no external hardware needed. Toggling `synthToggle` off removed the internal synth's contribution (de-doubling), confirming the requirement; the piano continues independently since MIDI-out routing is a separate, unaffected path (`midiSink`, not `synth`) — this is correct, not a bug.

  **Backlog item (out of scope for this change)**: user requested a UI toggle to mute/close the external MIDI-out device on demand — currently no such control exists, `openFirstAvailableDevice()` auto-opens silently with no way to stop it short of app exit. Belongs to `midi-output-routing` (Phase 5), not `internal-synth` (Phase 8). Logged to Engram for a future change proposal, not implemented here.

  **Process note**: user's local tooling auto-commits `Source/synth/*` on file save (commits `878e896`/`031de8e`/`4c49f78`/`93656e5` landed mid-session, outside this apply/verification workflow). One auto-commit (`93656e5` "feat: add LFO") captured a mid-listening-test intermediate state of `SynthPatch.h` (`lfoDepth=1.0`, `lfoDestination=cutoff`) rather than the final reverted default. The corrected default (`lfoDepth=0.0`, `lfoDestination=pitch`) is restored in the working tree as of this task's completion; per explicit user instruction, left uncommitted for their own auto-commit tooling to pick up rather than committed by this session.

## Phase 6: Final Cleanup & Verification

- [x] 6.1 Diff `Source/midi/*` and `Source/export/*` against `main`; confirm byte-for-byte unchanged across the full four-slice diff.

  **Discovery**: `main`'s HEAD (`aa1fe35`, "docs: add Berlin School generative sequencer proposal") is also the merge-base with this branch — `main` never received the `midi-export`/`midi-export-ui` merges (each phase has lived on its own long-running feature branch: `feat/midi-export`, `feat/midi-export-ui`, `feat/internal-synth`, none merged back to `main` yet). Diffing against literal `main` therefore shows `Source/midi/*`/`Source/export/*` as wholly new files, which is not the intended check. Used the correct base instead: `30d79c5` ("docs: plan internal-synth change"), the commit immediately preceding all Phase 8 work — `git diff 30d79c5 HEAD --stat -- Source/midi Source/export` is empty. Confirmed byte-for-byte unchanged, including the current uncommitted working tree.

- [x] 6.2 Diff `Source/core/*`, `Source/generation/*`, `Source/playback/*` against `main`; confirm byte-for-byte unchanged.

  Same base-commit correction as 6.1 applies. `git diff 30d79c5 HEAD --stat -- Source/core Source/generation Source/playback` is empty. Confirmed byte-for-byte unchanged, including the current uncommitted working tree (re-verified after task 6.10's rebuild).

- [x] 6.3 Grep the full diff for `TEMPORARY`/stub/placeholder comments; confirm zero remaining matches.

  Grepped `Source/synth/`, `Tests/Source/`, `Source/MainComponent.h`/`.cpp` for `TEMPORARY|TODO|FIXME|placeholder|stub|not.?implemented` (case-insensitive). One hit: `Tests/Source/SmokeTests.cpp:21` (`"harness placeholder assertion"`) — confirmed via `git diff 30d79c5 HEAD --stat -- Tests/Source/SmokeTests.cpp` (empty) to be a pre-existing file from an earlier phase, untouched by `internal-synth`. Zero matches introduced by this change.

- [x] 6.4 Run `BerlinTests.exe --category=Berlin`; confirm exit 0, including `DspLinkSmokeTests`, `SynthVoiceTests`, `LfoTests`, `SynthEngineTests`, and all pre-existing suites.

  Category name confirmed as `Berlin` (unchanged from Phase 1's confirmation). Ran twice: once before the final rebuild (6.10) and once after, against the freshly rebuilt binary. Both runs: exit code 0, 116/116 scenarios executed, 0 failures. Distinct suites present: `DeterministicRandom`, `DspLinkSmoke`, `Lfo`, `MidiExportTimeline`, `MidiMessageBytes`, `PitchGenerator`, `PlaybackTiming`, `Reproducibility`, `RhythmGenerator`, `Scale`, `Sequence`, `SequencePlayer`, `SequencePlayerStop`, `Smoke`, `Step`, `StepEvent`, `StepEventBuffer`, `SynthEngine`, `SynthVoice`, `Transport` — 20 suites, all pre-existing suites present alongside the four new Phase 8 suites, no regression.

- [x] 6.5 Code review against `design.md`'s nine numbered Architecture Decisions — see the full pass/fail table in this apply pass's report (all 9: PASS). Summary: (1) `SynthEngine` owns exactly one `SynthVoice voice;` by value, no pool/stealing — PASS. (2) `Lfo` is a hand-rolled, JUCE-free `double` phase accumulator; `SynthVoice::render` recomputes modulation once per `kControlBlockSize = 32` samples via `updateLfoModulation()`, never per-sample — PASS. (3) `SynthVoice::applyWaveform`'s pulse case calls `oscillator.initialise([this]{...}, 0)` (lookupTableNumPoints=0) reading the live `pulseWidth` member; `SynthVoice` carries `JUCE_DECLARE_NON_COPYABLE`/`JUCE_DECLARE_NON_MOVEABLE` — PASS (matches task 2.13's gate). (4) `SynthEngine::scratch` is a stereo `AudioBuffer<float>`; voice/effects render into scratch's own origin-0 space; only the terminal `destination.addFrom(ch, startSample, scratch, ch, 0, numSamples)` applies `startSample`; `numSamples` is clamped (`jmin`) with a `jassert`, never resized — PASS. (5) `SynthEngine::prepare`/`SynthVoice::prepare`/`SynthEffects::prepare` perform every allocating call (`scratch.setSize`, `Oscillator::initialise`/`prepare`, `StateVariableTPTFilter::prepare`, `ADSR::setSampleRate`/`setParameters`, `DelayLine::setMaximumDelayInSamples`/`prepare`, `Reverb::prepare`); all `render()`/`noteOn()`/`noteOff()`/`process()` paths are `noexcept` and allocation-free — PASS. (6) `enabled`/`wasEnabled` and `effectsEnabled`/`wasEffectsEnabled` are two independent atomic+latch pairs in `SynthEngine`, each resetting its owned state exactly once on the true→false edge; `SynthEffects` has no `enabled` flag of its own (bypass lives entirely in the caller's atomic, `dsp::Reverb::setEnabled` never used) — PASS. (7) `SynthEngine::render`'s segment loop calls `voice.noteOff()` only `if (event.note == currentNote)` — PASS. (8) `MainComponent::releaseResources` keeps the existing `player.flushPendingNoteOff` → MIDI path byte-unchanged and adds `synth.reset()`, which hard-resets voice, effects, scratch, and `currentNote` — PASS. (9) `MainComponent.h` declares `juce::ToggleButton synthToggle { "Synth" };`/`fxToggle { "FX" };` with in-class initialisers after the domain members; ctor does `addAndMakeVisible` + `onClick = [this]{...}` for both, `setSize(800, 600)` remains the last statement of the UI section; `resized()` lays out both via `removeFromTop`/`removeFromLeft` off `getLocalBounds().reduced(kMargin)` — PASS.

- [x] 6.6 RT-safety review: line-by-line confirmation that `SynthEngine::render`, `SynthVoice::render`/`noteOn`/`noteOff`, and `SynthEffects::process` contain no heap allocation, no lock acquisition, and no logging/formatting call anywhere in their call path.

  All five functions read line-by-line. `SynthEngine::render`: pointer arithmetic over `scratch`'s pre-sized channels, a bounded loop over the fixed-cap `StepEventBuffer`, calls into `voice.render`/`noteOn`/`noteOff`, `effects.process`/`reset`, and `destination.addFrom` — no allocation, no lock, no logging anywhere in the path. `SynthVoice::render`: control-block loop calling `updateLfoModulation()` (scalar math + `oscillator.setFrequency`/`filter.setCutoffFrequency`, both non-allocating parameter setters per design.md's verified-corrections table) then `oscillator.processSample`/`filter.processSample`/`adsr.getNextSample` per sample — no allocation, no lock, no logging. `noteOn`/`noteOff`: scalar field writes plus `adsr.noteOn()`/`noteOff()` (state-flag flips only) — clean. `SynthEffects::process`: per-sample `delayLine.popSample`/`pushSample` (ring-buffer read/write post-`prepare`) then `reverb.process(context)` — no allocation, no lock, no logging. Confirms the same gate `realtime-audio-wiring` already applies now holds for `Source/synth/`.

- [x] 6.7 Confirm the scoped convention exception's guardrails held: `Source/synth/SynthPatch.h` and `Source/synth/Lfo.h`/`.cpp` remain JUCE-free (no `#include <juce_...>`); no pre-existing JUCE-free file (`core/`, `generation/`, `playback/`, `midi/`, the tested half of `export/`) gained a JUCE include anywhere in this change.

  Read `SynthPatch.h` and `Lfo.h`/`.cpp` directly: zero `#include <juce_...>` in any of the three (only `<cmath>` in `Lfo.cpp`). Guardrail (b) — no pre-existing JUCE-free file gained a JUCE include — already established by task 6.1/6.2's empty diff against the Phase-8 start point (`30d79c5`): `core/`, `generation/`, `playback/`, `midi/`, `export/` have zero changes of any kind in this whole change, so no include could have been added.

- [x] 6.8 Apply the two MODIFIED delta specs to the live `openspec/specs/` tree: replaced `realtime-audio-wiring/spec.md`'s "Output Stays Silent" requirement with "Audio Output Governed By Synth Enable State" (4 scenarios) and "Standalone App Still Launches and Runs Silently" with "Standalone App Still Launches And Runs Without Dropouts" (2 scenarios); also corrected the spec's stale Purpose-line reference to unconditional silence, for internal consistency with the merged requirement. Replaced `unit-test-harness/spec.md`'s "Console Test Runner Project" requirement with the expanded module-list wording (`juce_dsp`/`juce_audio_formats`/`juce_audio_basics` alongside `juce_core`) and added the two new tier-split scenarios ("Synth DSP math is unit-tested while device-touching code is not", "Previously-excluded device/file-writing code stays excluded"), matching the precedent set by `midi-export` task 3.13 (in-place edit of the live spec file, not a new nested copy).

- [x] 6.9 Add the two new capability specs — `internal-synth-voice`, `internal-synth-output` — to the live `openspec/specs/` tree at `openspec/specs/internal-synth-voice/spec.md` and `openspec/specs/internal-synth-output/spec.md`, matching the existing `{capability}/spec.md` convention (verified against `deterministic-generation/spec.md`'s directory shape) — verbatim content from the change's own delta specs.

- [x] 6.10 Final full rebuild: `Berlin.sln` and `Tests/BerlinTests.sln`, both 0 errors. `Berlin.sln` (Debug|x64): 0 errors, 2 pre-existing unrelated warnings (`Main.cpp` unreferenced-parameter, unrelated to this change). `Tests/BerlinTests.sln` (Debug|x64): 0 errors. Post-rebuild `BerlinTests.exe --category=Berlin`: exit 0, 116/116 scenarios. No stray `Berlin.exe` processes were running before the build (checked via `tasklist`).
