# Tasks: Playback Transport & Step Clock

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~950-1150 authored (excl. generated `Builds/`/`JuceLibraryCode/`/`Tests/Builds/`/`Tests/JuceLibraryCode/`) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 StepEvent+StepEventBuffer -> PR2 Transport -> PR3 SequencePlayer+PlaybackTiming -> PR4 MainComponent wiring + final gate |
| Delivery strategy | ask-on-risk |
| Chain strategy | feature-branch-chain |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High — PR3 (SequencePlayer + the drift/timing suite) is the single riskiest unit and may itself need splitting once actual line counts are known (see note in the table below).

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | `StepEvent.h` + `StepEventBuffer.h` (header-only value types, no clock logic) | PR 1 (base: `main`) | `BerlinTests.exe --category=Berlin --name=StepEvent\|StepEventBuffer` | Console run, check exit code | Delete `Source/playback/{StepEvent,StepEventBuffer}.h` + their tests, revert both `.jucer` FILE entries for those two files |
| 2 | `Transport.h/.cpp` — the clock math (samples-per-step, boundary advance, drift) | PR 2 (base: PR1 branch) | `BerlinTests.exe --category=Berlin --name=Transport` | Same console run | Delete `Source/playback/Transport.*` + `TransportTests.cpp`, revert Transport FILE entries |
| 3 | `SequencePlayer.h/.cpp` + `PlaybackTimingTests.cpp` (event ordering, wrap, the falsifiable timing/drift deliverable) | PR 3 (base: PR2 branch) | `BerlinTests.exe --category=Berlin --name=SequencePlayer\|PlaybackTiming` | Same console run | Delete `Source/playback/SequencePlayer.*` + `SequencePlayerTests.cpp`/`PlaybackTimingTests.cpp`, revert SequencePlayer FILE entries |
| 4 | `MainComponent` wiring (first-ever touch) + final gate | PR 4 (base: PR3 branch) | `BerlinTests.exe --category=Berlin` (full) | Console run + `Berlin.exe` GUI manual launch (non-automatable gate) | Revert `Source/MainComponent.h/.cpp` to the stock Projucer template (byte-verifiable, per design's Migration/Rollout section); no `.jucer` FILE-list change to revert since no file is added/removed in this unit |

**Note on Unit 3's size**: `SequencePlayer.h/.cpp` alone is small (~70 lines), but `SequencePlayerTests.cpp` (ordering/wrap/inactive/overflow/playhead scenarios) plus `PlaybackTimingTests.cpp` (absolute-timestamp + long-run drift suites) are each estimated 100-200 lines. If the combined PR3 diff exceeds ~400 authored lines once code is written, split it into PR3a (`SequencePlayer` + `SequencePlayerTests.cpp`) and PR3b (`PlaybackTimingTests.cpp` alone, base: PR3a branch) — `PlaybackTimingTests.cpp` composes already-green `Transport`+`SequencePlayer` with no new production type (see Phase 5 below), so it is a clean split point.

## Phase 1: StepEvent (header-only aggregate — first file in the new `Source/playback/` tier)

- [x] 1.1 RED: Create `Tests/Source/StepEventTests.cpp` — construct a `StepEvent{sampleOffset, stepIndex, note, isNoteOn}` and read back each field; exercise the non-member `operator==`; a compile-time `static_assert (std::is_aggregate_v<berlin::StepEvent>)` inside the test file itself (fails to compile until the header exists, which **is** the RED proof here). Register `<FILE>` in `Tests/BerlinTests.jucer`'s existing `Source` group only (no `Berlin.jucer` entry yet — the production header doesn't exist). Run `Projucer.exe --resave Tests/BerlinTests.jucer`, build: confirm failure. Expected failure mode is a compile-time missing-include error (`"playback/StepEvent.h": No such file`), matching the pattern established in `core-sequencing-model` Phases 1-6 rather than a link error — note this if the actual failure differs. **Confirmed**: actual RED proof was `C1083: cannot open include file 'playback/StepEvent.h'`, exit code 1 — exactly the expected compile-time missing-include failure, same pattern as every phase in `core-sequencing-model`.
- [x] 1.2 GREEN: Create `Source/playback/StepEvent.h` — header-only, JUCE-free, exactly design.md's interface: `struct StepEvent { int sampleOffset=0; int stepIndex=0; int note=0; bool isNoteOn=false; };`, `static_assert (std::is_aggregate_v<StepEvent>);`, non-member `bool operator== (const StepEvent&, const StepEvent&);`. No `juce::MidiMessage`/`juce::MidiBuffer`/`juce_audio_basics` symbol anywhere in the file (review-check, also structurally guaranteed by staying JUCE-free). **Confirmed**: header contains only `<type_traits>` and the `berlin` namespace contents per design.md exactly; no JUCE symbol anywhere.
- [x] 1.3 Registration: add a new `<GROUP name="playback">` to `Berlin.jucer` (mirroring the `core`/`generation` groups' convention — brace-GUID id, header `compile="0"`) containing `<FILE file="Source/playback/StepEvent.h">`; add the mirrored `<GROUP name="playback">` to `Tests/BerlinTests.jucer` containing `<FILE file="../Source/playback/StepEvent.h">`. Run the **mandatory 6-step Projucer registration checklist**: (1) `<FILE>` in `Berlin.jucer`, (2) `<FILE>` in `Tests/BerlinTests.jucer` with `../` prefix, (3) `Projucer.exe --resave Berlin.jucer` exit 0, (4) `Projucer.exe --resave Tests/BerlinTests.jucer` exit 0, (5) sync-check: the `Source/(core|generation|playback)/*` entry sets match exactly modulo `../`, (6) build both `Berlin.sln` and `Tests/BerlinTests.sln`. Run `BerlinTests.exe --category=Berlin`: confirm exit 0 including the new StepEvent suite. **Confirmed**: both `--resave` exit 0; sync-check passed (`Source/playback/StepEvent.h` present in both `.jucer` files modulo `../`, `<MODULES>` unchanged in both — confirmed via `git diff --stat` showing only additive `<GROUP>`/`<FILE>` insertions); both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64) built clean, 0 errors; `BerlinTests.exe --category=Berlin` exit 0. **Deviation**: the StepEvent suite has 7 `beginTest` blocks, not the illustrative "34 existing + 1" estimate — construction/read-back, default-value read-back, and one equality test per field (mirrors `StepTests.cpp`'s per-field equality-test granularity) — giving 41 total `beginTest` blocks (34 existing + 7 new), all green.

## Phase 2: StepEventBuffer (header-only, fixed-capacity non-allocating sink; depends on StepEvent)

- [x] 2.1 RED: Create `Tests/Source/StepEventBufferTests.cpp` — push within capacity retains order; push at full capacity (64) drops the excess event, leaves stored events unchanged, and latches `hasOverflowed()`, with no allocation; `clear()` resets `size()` to 0 and `hasOverflowed()` to false. Register `<FILE>` in `Tests/BerlinTests.jucer`'s `Source` group only; `--resave` + build: confirm compile-time failure (missing `playback/StepEventBuffer.h`). **Confirmed**: actual RED proof was `C1083: cannot open include file 'playback/StepEventBuffer.h'`, MSBuild exit 1 (`BerlinTests_ConsoleApp.vcxproj` Debug|x64) — same compile-time missing-include pattern as every prior phase.
- [x] 2.2 GREEN: Create `Source/playback/StepEventBuffer.h` per design.md exactly: `static constexpr int capacity = 64;` `std::array<StepEvent, capacity>` backing, `count`, `overflowed`; `clear()`, `push()` (bool return), `size()`, `hasOverflowed()`, `operator[]` — all `noexcept`. **Confirmed**: header matches design.md's interface exactly, no allocation in `push()` (fixed `std::array` write only).
- [x] 2.3 Full 6-step registration checklist (add `<FILE file="Source/playback/StepEventBuffer.h">` to both `.jucer` playback groups, `../` prefix on the test side, `--resave` both, sync-check, build both). `BerlinTests.exe --category=Berlin`: exit 0. **Confirmed**: both `--resave` exit 0; sync-check passed (`Source/playback/StepEventBuffer.h` present in both `.jucer` files modulo `../`, `<MODULES>` byte-identical in both — confirmed via `git diff` showing only additive `<FILE>` insertions); both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64) built clean, 0 errors, 0 warnings; `BerlinTests.exe --category=Berlin` exit 0, 44 total `beginTest` blocks (41 prior + 3 new StepEventBuffer tests), 10 suites all green. **Deviation**: none — implementation matches design.md verbatim.

## Phase 3: Transport (JUCE-free step clock; `.h`/`.cpp` — first out-of-line file in this tier)

- [x] 3.1 RED: Create `Tests/Source/TransportTests.cpp` covering: default `bpm == 120`; `samplesPerStep == 5512.5` at `{44100, 120, 4}`; `bpm <= 0` never divides by zero or crashes; `advance()` reports no boundary when the block is shorter than `samplesPerStep`; a sequence of irregular-sized `advance()` calls lands each boundary's absolute sample position at `llround(k * samplesPerStep)` within ±1; a long-run (thousands-of-steps) drift check stays within ±1 of the ideal position across the whole run; stopped transport reports zero boundaries; unprepared transport (`prepare` never called) is safe and reports zero boundaries; boundary counting keeps increasing indefinitely across many blocks with no wraparound (this is `Transport`'s job per the reconciled spec — `SequencePlayer` owns the loop wrap, tested in Phase 4). **REVIEW-ONLY, not a runtime `UnitTest` scenario**: "no setter/method exists that changes `bpm` after construction" is an API-surface property — record it as a checklist line in this task, verified by inspecting `Transport`'s public interface during code review, not by a compile/runtime assertion (mirrors the non-runtime-testable pattern the constitution rule already uses for the audio-callback allocation check). Register `<FILE>` in `Tests/BerlinTests.jucer`'s `Source` group only; `--resave` + build: confirm compile-time failure (missing `playback/Transport.h`). **Confirmed**: actual RED proof was `C1083: cannot open include file 'playback/Transport.h'`, MSBuild exit 1 (`BerlinTests_ConsoleApp.vcxproj` Debug|x64) — same compile-time missing-include pattern as every prior phase. **Deviation**: `Transport` exposes no direct `getBpm()` accessor (none in design.md's interface), so "default bpm == 120" is verified indirectly through `prepare()`'s effect on `getSamplesPerStep()` rather than a direct field read — noted in the test file's own comment. Also, since boundary `k=0` fires at absolute sample 0 (the first step begins immediately at playback start, matching `SequencePlayer`'s Phase 4 process() ordering), the "block shorter than samplesPerStep reports no boundary" scenario first consumes boundary 0, then asserts the *next* boundary is not reported early — documented inline in the test.
- [x] 3.2 GREEN: Create `Source/playback/Transport.h` + `Source/playback/Transport.cpp` per design.md's exact interface: ctor `Transport(double bpm, int stepsPerBeat)`; `prepare(sampleRate)` computing `samplesPerStep = sampleRate*60/(bpm*stepsPerBeat)`, guarding `bpm<=0`/`stepsPerBeat<=0` as "never prepared" (`isPrepared() == samplesPerStep >= 1.0`); `reset()`, `start()`, `stop()`, `isRunning()`, `isPrepared()`, `getSamplesPerStep()`, `getSamplePosition()`, `getNextStepCounter()`; `countBoundaries(numSamples)` / `getBoundary(index)` as pure `const` queries, `advance(numSamples)` as the single commit — boundary *k* at absolute sample `llround(k*samplesPerStep)`; `advance` counts against the position **before** it moves. All methods `noexcept`. **Confirmed**: implementation matches design.md verbatim — no loop/wrap/playhead concept anywhere in `Transport`; `countBoundaries` is gated by `isPrepared()` (guards both div-by-zero in `prepare` and a runaway/infinite loop in `countBoundaries` when `samplesPerStep < 1.0`).
- [x] 3.3 Full 6-step registration checklist: `<FILE file="Source/playback/Transport.h" compile="0">` + `<FILE file="Source/playback/Transport.cpp" compile="1">` in `Berlin.jucer`'s playback group; matching `../`-prefixed pair in `Tests/BerlinTests.jucer`'s mirrored playback group; `TransportTests.cpp` as a `compile="1"` entry in `Tests/BerlinTests.jucer`'s own `Source` group. `--resave` both, sync-check (the `Source/playback/*` sets match modulo `../`; confirm no `<MODULE>` line changed in either file — an early instance of the gate enforced fully in Phase 7), build both. `BerlinTests.exe --category=Berlin`: exit 0. **Confirmed**: both `--resave` exit 0; sync-check passed (`Source/playback/Transport.{h,cpp}` present in both `.jucer` files modulo `../`, `<MODULES>` byte-identical in both — confirmed via `git diff` showing only additive `<FILE>` insertions); both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64) built clean, 0 errors, 0 warnings; `BerlinTests.exe --category=Berlin` exit 0, 54 total `beginTest` blocks (44 prior + 10 new Transport tests), 11 suites all green. Long-run drift (5000 boundaries) and irregular-block (200 boundaries) checks both landed exactly on `llround(k * samplesPerStep)` with zero error observed (the ±1 tolerance held with margin to spare, consistent with design.md's "structurally exactly 0 error" claim for the absolute-position formula). `Berlin.jucer` GUI app also rebuilt clean, 0 errors, 0 warnings, as a sanity check (MainComponent untouched — Phase 6 not yet reached).

## Phase 4: SequencePlayer (JUCE-free; depends on Transport + Sequence + StepEventBuffer)

- [ ] 4.1 RED: Create `Tests/Source/SequencePlayerTests.cpp` covering, against a fixed hand-built `Sequence` (not a generated one, so assertions are exact): note-off for an ending step is emitted **before** the note-on of the following step at a shared sample offset; an inactive step emits nothing; an empty `Sequence` (or a `Sequence` where every step is inactive) emits no events across any number of processed blocks; the wrap from step `N-1` to step `0` has no timing gap and no duplicated/dropped event; the exposed step position (`getPlayheadStep()`) wraps to `0` on completing `Sequence::size()` steps and keeps wrapping indefinitely rather than stopping after one pass (this is where the reconciled step-event-scheduling "Step Position Wraps Indefinitely" requirement is tested — `SequencePlayer`, not `Transport`, owns `% sequence.size()`); emitted note-on values match the seeded sequence's `note` fields, in order, across irregular/varying block sizes including some shorter than one step; `StepEventBuffer` overflow at extreme step-density interacts correctly (drop-and-flag, no crash); a compile-time `static_assert (noexcept (player.process (0, buf)))`. Register `<FILE>` in `Tests/BerlinTests.jucer`'s `Source` group only; `--resave` + build: confirm compile-time failure (missing `playback/SequencePlayer.h`).
- [ ] 4.2 GREEN: Create `Source/playback/SequencePlayer.h` + `Source/playback/SequencePlayer.cpp` per design.md's exact interface and `process()` body ordering: ctor `SequencePlayer(Sequence, Transport)` storing both **by value**; `prepare(sampleRate)` (forwards + resets, preserves running state), `start()`/`stop()`/`reset()`; `process(numSamples, StepEventBuffer&)` — `noexcept`, audio-thread, RT-safe — clearing `out` first, then for each `Transport::countBoundaries()`/`getBoundary()` pair: push pending note-off (if any) **before** computing `stepIndex = stepCounter % sequence.size()` and pushing that step's note-on (if `active`), storing the atomic playhead each iteration, then `transport.advance(numSamples)` once at the end; `getPlayheadStep()` as a relaxed atomic load. Member `std::atomic<int> playhead` makes the type non-copyable/non-movable by construction.
- [ ] 4.3 Full 6-step registration checklist: `<FILE file="Source/playback/SequencePlayer.h" compile="0">` + `<FILE file="Source/playback/SequencePlayer.cpp" compile="1">` in `Berlin.jucer`'s playback group; matching `../`-prefixed pair in `Tests/BerlinTests.jucer`'s mirrored playback group; `SequencePlayerTests.cpp` as a `compile="1"` entry in `Tests/BerlinTests.jucer`'s own `Source` group. `--resave` both, sync-check, build both. `BerlinTests.exe --category=Berlin`: exit 0.

## Phase 5: PlaybackTimingTests — the falsifiable "plays correctly" deliverable (composes Transport + SequencePlayer; no new production type)

- [ ] 5.1 Create `Tests/Source/PlaybackTimingTests.cpp`. **No RED/GREEN split is possible here** — same situation as `core-sequencing-model`'s Phase 7 Reproducibility suite: this test composes already-green `Transport` and `SequencePlayer` with no new header to be missing, so there is no compile-time failure to prove first. Write the suite directly: drive `SequencePlayer::process` over many simulated blocks with irregular, varying sizes (including some shorter than one step), summing block lengths into an absolute running position and rebasing each emitted event's `sampleOffset` against it; assert each boundary *k*'s absolute timestamp equals `llround(k * samplesPerStep)` within ±1 sample, spanning at least one full loop wrap; separately assert cumulative timing error over a thousands-of-steps, mixed-block-size run stays bounded (structurally exactly 0 by the absolute-position design, per design.md's drift decision) rather than merely small. If this suite fails on first run, the bug is in `Transport.cpp`/`SequencePlayer.cpp` from Phases 3-4, not a missing type — fix there, do not add a new file to satisfy this phase.
- [ ] 5.2 Register `<FILE>` for `PlaybackTimingTests.cpp` in `Tests/BerlinTests.jucer`'s `Source` group only (test-only file, no `Berlin.jucer` entry — mirrors how `ReproducibilityTests.cpp` was registered in the prior change). `Projucer.exe --resave Tests/BerlinTests.jucer`, build, run `BerlinTests.exe --category=Berlin`: confirm exit 0, including the new Playback Timing suite.

## Phase 6: MainComponent Wiring (first-ever touch to `Source/MainComponent.h/.cpp` across the whole project)

- [ ] 6.1 Pre-check: run `git diff --stat -- Source/MainComponent.h Source/MainComponent.cpp Source/Main.cpp` and confirm empty output — establishes the untouched baseline this phase is about to break for the first time (inverse of the check every prior change ran to confirm these files stayed untouched).
- [ ] 6.2 `MainComponent.cpp` — add an anonymous namespace with the accepted demo/seed constants (unresolved-by-design, treated as accepted defaults per orchestrator direction, not re-litigated here): `kBpm = 120.0`, `kStepsPerBeat = 4`, `kNumSteps = 16`, `kSeed = 12345`. Add `static berlin::Sequence buildSeededSequence()`: `DeterministicRandom rng(kSeed);` → `RhythmGenerator(kNumSteps, 0.5f).generate(rng)` → for each active step, `PitchGenerator pitch(Scale::minor(48), 36, 72)` assigns `.note` via `generateNextNote(rng)` → return the `Sequence`. Message-thread only; allocation allowed here (this is the one non-audio-thread codepath in the whole change).
- [ ] 6.3 `MainComponent.h` — add exactly three things, nothing else: `#include "playback/SequencePlayer.h"`, `#include "playback/StepEventBuffer.h"` (file-relative from `Source/`, no `headerPath` change needed); the `static berlin::Sequence buildSeededSequence();` declaration; two private members, `berlin::SequencePlayer player;` and `berlin::StepEventBuffer blockEvents;`. `paint`/`resized`/`releaseResources`/dtor declarations and `JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR` stay untouched.
- [ ] 6.4 `MainComponent.cpp` constructor — change the initialiser list to `: player (buildSeededSequence(), berlin::Transport (kBpm, kStepsPerBeat))` (runs before the constructor body, therefore before `setAudioChannels`; `player`'s atomic member makes it non-movable, so the compiler — not a comment — enforces this ordering). In the body, add `player.start();` immediately after the existing `setSize (800, 600);` line and **before** the `RuntimePermissions`/`setAudioChannels` if/else block (so it runs regardless of which branch executes). Nothing else in the constructor moves or changes.
- [ ] 6.5 `MainComponent.cpp` `prepareToPlay` — replace the stock comment-only body with `juce::ignoreUnused (samplesPerBlockExpected); player.prepare (sampleRate);`. This runs on the audio thread per the stock JUCE comment; `prepare()` is arithmetic-only, no allocation.
- [ ] 6.6 `MainComponent.cpp` `getNextAudioBlock` — keep `bufferToFill.clearActiveBufferRegion();` as the **first** statement (silence stays the output), then add `player.process (bufferToFill.numSamples, blockEvents);` immediately after it. `blockEvents` is consumed by nobody this phase (Phase 5/8 MIDI/synth work). `releaseResources`, `paint`, `resized`, and the destructor stay completely unchanged — must be absent from the diff.
- [ ] 6.7 Verification gate — confirm only the documented lines changed: run `git diff -- Source/MainComponent.h Source/MainComponent.cpp` and check line-by-line against the six edits above (design.md's per-method modification table). Confirm `paint`, `resized`, `releaseResources`, and the destructor are byte-identical to the stock scaffold, and no stray include/member/method was introduced. This is **not** a zero-diff check (this phase is the first one that legitimately touches these files) — it is a "diff matches exactly the documented plan, nothing more" check.
- [ ] 6.8 No `.jucer` FILE-list edit is needed for `Source/MainComponent.h/.cpp` themselves (they are pre-existing, already-registered files whose content changed, not new/renamed/deleted files — the 6-step registration checklist applies to file adds, not content edits). Confirm this by checking neither `.jucer`'s `<FILE name="MainComponent...">` entries changed.
- [ ] 6.9 Build `Berlin.jucer`'s GUI app project (`Builds/VisualStudio2026/Berlin.sln`, Debug|x64): confirm 0 errors (pre-existing benign `C4100` unreferenced-parameter warnings in `Source/Main.cpp`/`MainComponent.cpp` are expected and unrelated). `Tests/BerlinTests.jucer` does not compile `MainComponent.cpp` at all (design.md: `AudioSourceChannelInfo`/`AudioBuffer` never cross into the tested surface) — re-run `BerlinTests.exe --category=Berlin` purely as a sanity check that this phase touched nothing the test project depends on: confirm exit 0, same suite count as end of Phase 5.

## Phase 7: Final Gate

- [ ] 7.1 `Projucer.exe --resave Berlin.jucer`: confirm exit 0.
- [ ] 7.2 `Projucer.exe --resave Tests/BerlinTests.jucer`: confirm exit 0.
- [ ] 7.3 Sync-check: the `Source/(core|generation|playback)/*` `<FILE>` entry sets in both `.jucer` files match exactly modulo the `../` prefix.
- [ ] 7.4 **`<MODULES>` gate (defect, not warning, if violated)**: diff `Tests/BerlinTests.jucer`'s `<MODULES>` block before vs. after this whole change and confirm it is byte-identical — `juce_core` only, no addition. This is the concrete, explicit check the testability boundary confirmed by design.md depends on; do not treat a `<MODULES>` change as acceptable collateral from any other task in this file.
- [ ] 7.5 Build both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64): confirm 0 errors/0 warnings beyond the pre-existing benign `C4100` warnings noted in Phase 6.9.
- [ ] 7.6 Run `BerlinTests.exe --category=Berlin`: confirm exit 0, previous 34 `beginTest` blocks plus the five new suites (StepEvent, StepEventBuffer, Transport, SequencePlayer, PlaybackTiming) all present and green.
- [ ] 7.7 `git diff --stat -- Source/Main.cpp`: confirm empty (untouched, per design.md's File Changes table).
- [ ] 7.8 `git diff --stat` overall: confirm only the file set from design.md's File Changes table appears (`Source/playback/*`, `Source/MainComponent.h/.cpp`, `Tests/Source/{StepEvent,StepEventBuffer,Transport,SequencePlayer,PlaybackTiming}Tests.cpp`, `Berlin.jucer`, `Tests/BerlinTests.jucer`, regenerated `Builds/`/`JuceLibraryCode/`/`Tests/Builds/`/`Tests/JuceLibraryCode/`, plus `openspec/changes/playback-transport-clock/*` planning artifacts) — no stray/unplanned files, and specifically confirm `Source/core/`, `Source/generation/` show no diff.
- [ ] 7.9 **Review-only gate**: inspect `getNextAudioBlock` and the full `process()` call path (`Transport`/`SequencePlayer`) line by line and confirm no heap allocation, no lock acquisition, and no `juce::String`/`Logger` call anywhere in it — this is a structural property the constitution rule requires but C++ offers no runtime assertion for; a human/reviewer checklist item, not something `BerlinTests.exe` can prove.
- [ ] 7.10 **Review-only gate**: inspect `Transport`'s public interface and confirm no setter/method exists that mutates `bpm` after construction (API-surface property, same non-runtime-testable class as 7.9).
- [ ] 7.11 **MANUAL, NOT AUTOMATABLE — human step, not an `sdd-apply` step**: launch the built standalone `Berlin.exe` manually. Confirm it opens its audio device and runs the transport continuously and silently, with no dropouts, asserts, or crashes, until manually closed. This is the realtime-audio-wiring spec's "Manual launch gate passes" scenario and the design's stated `MainComponent`-glue coverage gap — it cannot be reached by the `juce_core`-only headless harness, and no amount of additional `UnitTest` code closes it.

### Full Suite Confirmation (fill in at execution time)
`BerlinTests.exe --category=Berlin` exit 0, all 13 suites (Smoke, Step, Sequence, Scale, DeterministicRandom, PitchGenerator, RhythmGenerator, Reproducibility, StepEvent, StepEventBuffer, Transport, SequencePlayer, PlaybackTiming) green. `Berlin.jucer` GUI app rebuilt clean, 0 errors. Standalone manual launch gate passed (human-confirmed, not exit-code-confirmed).
