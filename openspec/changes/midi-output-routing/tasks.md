# Tasks: MIDI Output Routing

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~550-750 authored (excl. generated `Builds/`/`JuceLibraryCode/`/`Tests/Builds/`/`Tests/JuceLibraryCode/`) |
| 400-line budget risk | Low-Med |
| Chained PRs recommended | Yes |
| Suggested split | PR1 flushPendingNoteOff -> PR2 MidiMessageBytes -> PR3 MidiEventTranslator -> PR4 MidiOutputSink -> PR5 MainComponent wiring + final gate |
| Delivery strategy | ask-on-risk |
| Chain strategy | feature-branch-chain |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: Low-Med — no single unit is expected to approach 400 authored lines; `MidiOutputSink` (Phase 4) is the most logic-dense untestable unit and the one most worth a standalone PR for reviewer attention given the close-sequence and timestamp-anchor decisions it carries.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | `SequencePlayer::flushPendingNoteOff` (JUCE-free, additive debt fix) | PR 1 (base: `main`) | `BerlinTests.exe --category=Berlin --name=SequencePlayerStop` | Console run, check exit code | Delete `Tests/Source/SequencePlayerStopTests.cpp`, revert its `<FILE>` entry, revert the additive method from `SequencePlayer.h/.cpp` |
| 2 | `Source/midi/MidiMessageBytes.h` (JUCE-free packing helper — first file in the new `midi/` tier) | PR 2 (base: PR1 branch) | `BerlinTests.exe --category=Berlin --name=MidiMessageBytes` | Same console run | Delete `Source/midi/MidiMessageBytes.h` + its test, revert both `.jucer` `<FILE>` entries (the new `midi` group in both files) |
| 3 | `Source/midi/MidiEventTranslator.h/.cpp` (JUCE-aware, manual-gate-only) | PR 3 (base: PR2 branch) | None automatable — reviewer inspection only | N/A (no test harness reaches this) | Delete both files, revert `Berlin.jucer`'s `<FILE>` entries (no `Tests/BerlinTests.jucer` entry exists to revert) |
| 4 | `Source/midi/MidiOutputSink.h/.cpp` (JUCE-aware, manual-gate-only) | PR 4 (base: PR3 branch) | None automatable — reviewer inspection only | N/A | Delete both files, revert `Berlin.jucer`'s `<FILE>` entries |
| 5 | `MainComponent` wiring + Final Gate | PR 5 (base: PR4 branch) | `BerlinTests.exe --category=Berlin` (full) | Console run + manual virtual-MIDI-port monitor capture (non-automatable gate) | Revert `Source/MainComponent.h/.cpp` to the archived `playback-transport-clock` state (byte-verifiable) |

## Phase 1: SequencePlayer::flushPendingNoteOff (JUCE-free debt-first fix — TDD'd before any JUCE MIDI type is touched)

- [x] 1.1 RED: create `Tests/Source/SequencePlayerStopTests.cpp` covering the 5 scenarios from the `step-event-scheduling` delta spec: emits exactly one note-off for a currently-sounding note at sample offset 0 and clears the pending-note state; is a no-op (buffer ends cleared, nothing pushed) when nothing is sounding; is idempotent (a second call with no processing in between emits nothing); ordering against `reset()` (flush-before-reset emits, reset-before-flush emits nothing because `reset()` already discarded the pending-note state); no unmatched note-on across repeated start/process/flush/stop cycles, including a stop that lands mid-step and a stop with nothing sounding. Use a hand-built `Sequence` for exact assertions (same convention as `SequencePlayerTests.cpp`). Include a compile-time `static_assert (noexcept (player.flushPendingNoteOff (buf)))`. Register `<FILE>` in `Tests/BerlinTests.jucer`'s `Source` group only (the method doesn't exist on the production class yet). `--resave`, build: confirm compile-time failure (expected: "no member named `flushPendingNoteOff`" or equivalent). **Confirmed**: `Projucer.exe --resave Tests/BerlinTests.jucer` exit 0; `MSBuild.exe Tests/Builds/VisualStudio2026/BerlinTests.sln` failed with 18× `C2039: "flushPendingNoteOff": no es un miembro de "berlin::SequencePlayer"` — exactly the expected missing-member compile-time failure, same class of RED proof as every prior phase's missing-include pattern (here a missing-member, since `SequencePlayer.h` already exists and is only gaining a method).
- [x] 1.2 GREEN: add `bool flushPendingNoteOff (StepEventBuffer& out) noexcept;` to `Source/playback/SequencePlayer.h` and implement in `.cpp` exactly per design's Decision 2 contract — the single emitting operation: clears `out` first (same sink-ownership convention as `process()`), pushes at most one `StepEvent { 0, pendingStep, pendingNote, false }` if `pendingNote >= 0`, sets `pendingNote = -1`, leaves `pendingStep` untouched, returns whether it emitted. No overload is added to `stop()`/`reset()`; both signatures stay unchanged and neither emits anything itself. **Confirmed**: implementation matches design.md's Decision 2 contract verbatim; `stop()`/`reset()`/`process()` bodies byte-unchanged (only `flushPendingNoteOff` added, plus its declaration/doc comment in the header).
- [x] 1.3 Build + regression: rebuild `Tests/BerlinTests.sln`, run `BerlinTests.exe --category=Berlin`: confirm exit 0, previous suite count plus the new `SequencePlayerStop` suite. No `.jucer` FILE-list edit is needed for `SequencePlayer.h/.cpp` itself (a pre-existing, already-registered file whose content changed) — only the new test file's own entry from 1.1 applies. **Confirmed**: `MSBuild.exe Tests/Builds/VisualStudio2026/BerlinTests.sln -p:Configuration=Debug -p:Platform=x64` exit 0, 0 errors, 0 warnings. `BerlinTests.exe --category=Berlin` exit 0, "All tests completed successfully", 73 total `beginTest` blocks (67 prior + 6 new `SequencePlayerStop` tests), 14 suites all green. `git diff --stat -- Source/playback/SequencePlayer.h Source/playback/SequencePlayer.cpp` shows only additive lines (no line removed/changed in `stop()`/`reset()`/`process()`).

## Phase 2: MidiMessageBytes.h (JUCE-free packing helper — first file in the new `Source/midi/` tier)

- [x] 2.1 RED: create `Tests/Source/MidiMessageBytesTests.cpp`, table-driven per the `midi-output-dispatch` spec's two scenarios: correct status bytes for `makeNoteOn` (`0x90|(channel-1)`) and `makeNoteOff` (`0x80|(channel-1)`, release velocity `0` — a REAL note-off, not note-on with velocity 0); `makeAllNotesOff` (`0xB0|(channel-1), 123, 0`); channel `1` and `16` endpoints; channel `0`/`17`/`-1` clamp via `clampMidiChannel`; note/velocity `128` and `-1` clamp via `clampMidiData`. Include compile-time `static_assert`s: `std::is_aggregate_v<MidiMessageBytes>`; `makeNoteOn (1, 60, 100) == MidiMessageBytes { 0x90, 60, 100 }` (both `constexpr`). Register `<FILE>` in `Tests/BerlinTests.jucer`'s `Source` group only. `--resave`, build: confirm compile-time failure (missing `midi/MidiMessageBytes.h` include). **Confirmed**: `Projucer.exe --resave Tests/BerlinTests.jucer` exit 0; `MSBuild.exe Tests/Builds/VisualStudio2026/BerlinTests.sln` failed with `C1083: No se puede abrir el archivo incluir: 'midi/MidiMessageBytes.h': No such file or directory`, exit 1 error — exactly the expected compile-time missing-include failure.
- [x] 2.2 GREEN: create `Source/midi/MidiMessageBytes.h` exactly per design's interface: aggregate `struct MidiMessageBytes { unsigned char status, data1, data2; }`, non-member `operator==` (keeps it an aggregate), `inline constexpr int kAllNotesOffController = 123;`, `constexpr clampMidiChannel` (`[1, 16]`), `constexpr clampMidiData` (`[0, 127]`), `constexpr makeNoteOn`, `constexpr makeNoteOff` (real `0x80` status), `constexpr makeAllNotesOff`. Header-only, no `juce_audio_basics`/JUCE symbol anywhere in the file (review-check, also structurally guaranteed by staying JUCE-free). **Confirmed**: header matches design.md's interface verbatim; grepped the file for `juce`/`Juce`/`JUCE` — 2 matches, both prose in the top doc-comment ("JUCE-free constexpr..." and a mention of `juce::MidiMessage` explaining Decision 1's rationale for downstream callers), zero `#include` of any JUCE header and zero actual JUCE type/symbol used in code — confirming JUCE-free in the structural sense the spec requires.
- [x] 2.3 Full 6-step registration checklist — this file establishes the new `<GROUP name="midi">` in both projects: (1) add `<GROUP name="midi">` to `Berlin.jucer` with `<FILE id="mdMsgH" file="Source/midi/MidiMessageBytes.h" compile="0">`. (2) add the mirrored `<GROUP name="midi">` to `Tests/BerlinTests.jucer` with `<FILE id="tmMsgH" file="../Source/midi/MidiMessageBytes.h" compile="0">` — this is the ONLY `midi`-group entry `Tests/BerlinTests.jucer` ever gets in this whole change, per design's Projucer Registration Mechanics (`MidiEventTranslator`/`MidiOutputSink` stay JUCE-aware and out of the test project). (3) `Projucer.exe --resave Berlin.jucer` exit 0. (4) `Projucer.exe --resave Tests/BerlinTests.jucer` exit 0. (5) sync-check: confirm no `<MODULE>` line changed in either file. (6) build both `Berlin.sln` and `Tests/BerlinTests.sln`. Run `BerlinTests.exe --category=Berlin`: confirm exit 0 including the new `MidiMessageBytes` suite. **Confirmed**: both `--resave` exit 0; `git diff -- Berlin.jucer Tests/BerlinTests.jucer | grep -E "^[+-].*<MODULE "` returned zero lines (exit 1, no match) — no `<MODULE>` change in either file. `Berlin.sln` (Debug|x64) built clean, 0 errors, only the 2 pre-existing benign `C4100` warnings in `Source/Main.cpp`. `Tests/BerlinTests.sln` (Debug|x64) built clean, 0 errors, 0 warnings. `BerlinTests.exe --category=Berlin` exit 0, "All tests completed successfully", 80 total `beginTest` blocks (73 prior + 7 new `MidiMessageBytes` tests), 15 suites all green.

## Phase 3: MidiEventTranslator (JUCE-aware — `StepEventBuffer` -> `juce::MidiBuffer`; manual-gate-only, no automated test possible)

- [x] 3.1 No RED/GREEN split is possible here — same situation as `playback-transport-clock`'s Phase 5 (`PlaybackTimingTests`): this file needs `juce_audio_basics`, which is an explicit non-goal for `Tests/BerlinTests.jucer`. Write `Source/midi/MidiEventTranslator.h/.cpp` directly, per design's exact interface: `explicit MidiEventTranslator (int outputChannel) noexcept;` (channel clamped `[1,16]` via `clampMidiChannel` at construction); `static constexpr int kNoteVelocity = 100;`; `void translate (const StepEventBuffer& events, juce::MidiBuffer& destination) const noexcept;` — `destination.clear()` first, then one `juce::MidiMessage (status, data1, data2)` per `StepEvent`, built FROM `MidiMessageBytes` per Decision 1 (never `juce::MidiMessage::noteOn`/`noteOff` directly, so the unit-tested packing helper covers real shipped behaviour), at timestamp = `StepEvent::sampleOffset` unmodified — `bufferToFill.startSample` is NOT added (Phase 4's warning applied to audio-buffer indexing only, not here); never calls `ensureSize` inside `translate`; `int getChannel() const noexcept;`. **Confirmed**: `Source/midi/MidiEventTranslator.h/.cpp` created matching design's interface verbatim — out-of-line definitions in `.cpp` (same "`.h`/`.cpp` for types with out-of-line definitions" convention as `SequencePlayer`/`Transport`, so a forgotten `<FILE>` registration fails loudly as a link error). `translate()` builds `juce::MidiMessage (bytes.status, bytes.data1, bytes.data2)` from `berlin::makeNoteOn`/`makeNoteOff` — never `juce::MidiMessage::noteOn`/`noteOff` directly. No `ensureSize` call anywhere in the file (grepped, zero matches). `event.sampleOffset` passed to `addEvent` unmodified, no `startSample` addition anywhere.
- [x] 3.2 Registration (adapted 6-step checklist — this file has NO `Tests/BerlinTests.jucer` counterpart by design): (1) add `<FILE id="mdTrnH" file="Source/midi/MidiEventTranslator.h" compile="0">` and `<FILE id="mdTrnC" file="Source/midi/MidiEventTranslator.cpp" compile="1">` to `Berlin.jucer`'s `midi` group. (2) **N/A** — no `Tests/BerlinTests.jucer` entry for this file; record this explicitly rather than skipping the step silently (design: "the test project gets no such alarm... checklist step 5 is the sole guard there"). (3) `Projucer.exe --resave Berlin.jucer` exit 0. (4) `Projucer.exe --resave Tests/BerlinTests.jucer` exit 0 (a no-op for this file — confirms nothing leaked into the test project). (5) sync-check: confirm no `<MODULE>` line changed in either file, AND confirm `MidiEventTranslator.{h,cpp}` do NOT appear anywhere in `Tests/BerlinTests.jucer` — treat their presence there as a defect. (6) build both `Berlin.sln` and `Tests/BerlinTests.sln`. **Confirmed**: both `--resave` exit 0; `git diff -- Berlin.jucer Tests/BerlinTests.jucer | grep -E "^[+-].*<MODULE "` returned zero lines — no `<MODULE>` change. `grep -c "MidiEventTranslator" Tests/BerlinTests.jucer` returned `0` — confirmed absent from the test project, exactly the designed asymmetry, not a defect. Both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64) built clean.
- [x] 3.3 Build `Berlin.sln`: confirm 0 errors. Run `BerlinTests.exe --category=Berlin` as a regression sanity check only (`Tests/BerlinTests.jucer` never compiles this file): confirm exit 0, same suite count as end of Phase 2. **Confirmed**: `Berlin.sln` (Debug|x64) built clean, 0 errors, 0 warnings (the new unreferenced `MidiEventTranslator`/`.cpp` produced no warning). `Tests/BerlinTests.sln` (Debug|x64) built clean, 0 errors, 0 warnings; `BerlinTests.exe --category=Berlin` exit 0, "All tests completed successfully", 80 total `beginTest` blocks — identical to Phase 2's end count, confirming no regression and no leakage into the test project.

## Phase 4: MidiOutputSink (JUCE-aware — device lifecycle, dispatch, timestamp anchor, panic guard; manual-gate-only)

- [x] 4.1 No RED/GREEN split possible (`juce_audio_devices`, same constraint as Phase 3). Write `Source/midi/MidiOutputSink.h/.cpp` directly, per design's exact interface and the six Architecture Decisions it embodies:
  - ctor `explicit MidiOutputSink (int outputChannel) noexcept;` (channel clamped `[1,16]`); dtor calls `closeDevice()`.
  - message-thread-only: `bool openFirstAvailableDevice();` (`getAvailableDevices()[0].identifier` -> `openDevice` + `startBackgroundThread()`; `false` = none available / open failed — the valid degraded state, not an error); `bool isDeviceOpen() const noexcept;`; `juce::String getOpenDeviceName() const;` (never called from the callback).
  - `void closeDevice() noexcept;` — implement Decision 4's EXACT four-step order, each its own checked sub-step: (a) `device->clearAllPendingMessages()`; (b) `device->sendMessageNow (...)` with the CC 123 message packed via `makeAllNotesOff (channel)`; (c) `device->stopBackgroundThread()`; (d) `device.reset()`. Idempotent when already closed (early-return if `device == nullptr`). **Verify at apply**: confirm `juce::MidiOutput::clearAllPendingMessages()` exists in the pinned JUCE version; if absent, substitute the order to `stopBackgroundThread()` -> `sendMessageNow (CC123)` and record the substitution here per design's Open Questions.
  - `void prepare (double sampleRateToUse) noexcept;` — Decision 5: sets `sampleRate`, zeroes `elapsedSamples`, captures `startMs = juce::Time::getMillisecondCounterHiRes()` ONCE per call (not per block).
  - `void dispatch (const juce::MidiBuffer& buffer, int numSamples) noexcept;` — Decision 6 + the exact ordering contract: if `device != nullptr && sampleRate > 0.0 && ! buffer.isEmpty()`, call `device->sendBlockOfMessages (buffer, startMs + elapsedSamples * 1000.0 / sampleRate, sampleRate)`; then `elapsedSamples += numSamples` UNCONDITIONALLY (even with no device, even on an empty buffer) — this ordering (guarded send, unconditional counter advance) is load-bearing; implement it exactly as shown, not as an early return that skips the counter update.
  - `void sendImmediately (const juce::MidiBuffer& buffer) noexcept;` — `sendMessageNow` per message, used only from `releaseResources`'s shutdown path, never the callback.
  - private members exactly: `std::unique_ptr<juce::MidiOutput> device; const int channel; double sampleRate { 0.0 }; double startMs { 0.0 }; long long elapsedSamples { 0 };`

  **Verified against the pinned JUCE checkout (`H:\Proyectos\Juce\Projucer\modules\juce_audio_devices\midi_io\juce_MidiDevices.h`), NOT just design.md's flagged assumption**: `void clearAllPendingMessages() { outputThread.clearAllPendingMessages(); }` (line 428) exists exactly as design assumed, alongside `sendMessageNow`, `sendBlockOfMessages`, `startBackgroundThread`, `stopBackgroundThread`, `getAvailableDevices`, `openDevice`, all with the signatures design.md's Interfaces/Contracts section assumed. **No fallback substitution was needed** — the unverified-API risk flagged in design.md's Open Questions and Decision 4 does not materialize in this project's pinned JUCE version. Implemented the exact four-step order (1) `clearAllPendingMessages()` (2) `sendMessageNow(CC123)` (3) `stopBackgroundThread()` (4) `device.reset()`, unchanged from design, with an inline comment in `closeDevice()` explaining why steps 1/2 cannot be swapped (a still-queued future note-on would overtake the panic guard and hang the exact note it exists to prevent). `MidiMessage(int,int,int) noexcept` (juce_MidiMessage.h line 58), `MidiBuffer::addEvent`/`clear`/`isEmpty` (juce_MidiBuffer.h), and `Time::getMillisecondCounterHiRes()` (juce_Time.h line 348) were all independently confirmed to exist with the assumed signatures before being used in `MidiEventTranslator.cpp`/`MidiOutputSink.cpp`.
- [x] 4.2 RT-safety inventory review (review-only, not automatable — same class of check as the constitution rule): confirm `dispatch` and its `sendBlockOfMessages` call site contain no `ensureSize`, no `juce::String`, no `juce::Logger`, no `Time::getMillisecondCounter*` call, no `new`/`delete`, no file/enumeration call; confirm the only lock is the one inside `sendBlockOfMessages` itself. **Confirmed**: grepped `MidiOutputSink.cpp` for `ensureSize|juce::String|juce::Logger|getMillisecondCounter\(\)|new |delete |CriticalSection|Lock` — the only hit is `juce::String` in `getOpenDeviceName()`, which is documented "NEVER from the callback" and is not called from `dispatch`/`prepare`/the audio path. `dispatch()` body contains only the guarded `sendBlockOfMessages` call and unconditional `elapsedSamples += numSamples`; no allocation, no logging, no wall-clock read, no lock outside `sendBlockOfMessages`'s own internal `CriticalSection` (the one named exception recorded in design.md's Constitution Exception section).
- [x] 4.3 Registration (adapted 6-step checklist, same asymmetry as Phase 3): (1) add `<FILE id="mdSnkH" file="Source/midi/MidiOutputSink.h" compile="0">` and `<FILE id="mdSnkC" file="Source/midi/MidiOutputSink.cpp" compile="1">` to `Berlin.jucer`'s `midi` group. (2) **N/A** — no `Tests/BerlinTests.jucer` entry; confirm neither file appears there. (3) `Projucer.exe --resave Berlin.jucer` exit 0. (4) `Projucer.exe --resave Tests/BerlinTests.jucer` exit 0 (no-op). (5) sync-check: no `<MODULE>` line changed in either file. (6) build both solutions. **Confirmed**: both `--resave` exit 0; `git diff -- Berlin.jucer Tests/BerlinTests.jucer | grep -E "^[+-].*<MODULE "` returned zero lines. `grep -c "MidiOutputSink" Tests/BerlinTests.jucer` returned `0` — confirmed absent, matching the designed asymmetry.
- [x] 4.4 Build `Berlin.sln`: confirm 0 errors. Run `BerlinTests.exe --category=Berlin` as a regression sanity check: confirm exit 0, same suite count as end of Phase 2/3. **Confirmed**: `Berlin.sln` (Debug|x64) built clean, 0 errors, 0 warnings (`clearAllPendingMessages()`, `sendBlockOfMessages`, `startBackgroundThread`/`stopBackgroundThread`, `getAvailableDevices`/`openDevice` all resolved and compiled without any diagnostic). `Tests/BerlinTests.sln` (Debug|x64) built clean, 0 errors, 0 warnings; `BerlinTests.exe --category=Berlin` exit 0, 80 total `beginTest` blocks — identical to Phase 2/3's end count, no regression, no leakage into the test project.

## Phase 5: MainComponent Wiring (second-ever touch to `Source/MainComponent.h/.cpp`, after `playback-transport-clock`'s Phase 6)

- [x] 5.1 Pre-check: run `git diff --stat -- Source/MainComponent.h Source/MainComponent.cpp` against the archived `playback-transport-clock` baseline and confirm empty — establishes the untouched-since-archive baseline this phase is about to change for the first time in this new change. **Confirmed**: `git diff --stat -- Source/MainComponent.h Source/MainComponent.cpp` was empty before any edit in this phase.
- [x] 5.2 `MainComponent.h` — add exactly: `#include "midi/MidiEventTranslator.h"`, `#include "midi/MidiOutputSink.h"`; three new private members `berlin::MidiEventTranslator midiTranslator;`, `berlin::MidiOutputSink midiSink;`, `juce::MidiBuffer midiBlock;`. Nothing else changes; `paint`/`resized` stay absent from the diff. **Confirmed**: exactly these two includes and three members added; `paint`/`resized` declarations byte-identical to baseline.
- [x] 5.3 `MainComponent.cpp` anonymous namespace — add `constexpr int kMidiChannel = 1;` and `constexpr int kMidiBufferBytes = 1024;` alongside the existing demo constants. **Confirmed**: both constants added verbatim alongside `kBpm`/`kStepsPerBeat`/`kNumSteps`/`kSeed`.
- [x] 5.4 `MainComponent.cpp` constructor — append `, midiTranslator (kMidiChannel), midiSink (kMidiChannel)` to the initialiser list; in the body, immediately after `player.start();` and **before** the `RuntimePermissions`/`setAudioChannels` if/else block, add `midiSink.openFirstAvailableDevice();` (return value deliberately ignored — `false` is the valid silent state, not an error to surface). **Confirmed**: initialiser list and `midiSink.openFirstAvailableDevice();` placement match exactly; `RuntimePermissions`/`setAudioChannels` if/else block byte-identical to baseline, unmoved.
- [x] 5.5 `MainComponent.cpp` `prepareToPlay` — after `player.prepare (sampleRate);` add `midiSink.prepare (sampleRate); midiBlock.ensureSize (kMidiBufferBytes);` — the only `ensureSize` call in the whole change. **Confirmed**: added exactly as specified; grepped the whole `Source/midi/` + `Source/MainComponent.cpp` diff for `ensureSize` — the only match is this one line.
- [x] 5.6 `MainComponent.cpp` `getNextAudioBlock` — keep `bufferToFill.clearActiveBufferRegion();` first, then after `player.process (...)` add `midiTranslator.translate (blockEvents, midiBlock); midiSink.dispatch (midiBlock, bufferToFill.numSamples);`. Confirm `bufferToFill.startSample` is NOT added to any MIDI timestamp anywhere in this call path (design's stated most-likely apply-time bug). **Confirmed**: `clearActiveBufferRegion()` stays first; `translate`/`dispatch` added immediately after `player.process(...)`; grepped the whole `getNextAudioBlock` body and `MidiEventTranslator.cpp`/`MidiOutputSink.cpp` for `startSample` — zero matches, confirming it is never added to a MIDI timestamp.
- [x] 5.7 `MainComponent.cpp` `releaseResources` — was empty (per `playback-transport-clock`'s Phase 4); becomes `if (player.flushPendingNoteOff (blockEvents)) { midiTranslator.translate (blockEvents, midiBlock); midiSink.sendImmediately (midiBlock); }`. **Must NOT call `player.stop()`** (Decision 3 — preserves the device-restart-resumes behaviour `playback-transport-clock` established). **Confirmed**: implemented exactly as specified, with an inline comment explaining Decision 3; grepped the new `releaseResources` body for `player.stop` — zero matches.
- [x] 5.8 `MainComponent.cpp` destructor — `shutdownAudio();` stays first; add `midiSink.closeDevice();` immediately after it. **Confirmed**: `shutdownAudio();` unchanged as the first statement, `midiSink.closeDevice();` added immediately after.
- [x] 5.9 Verification gate — `git diff -- Source/MainComponent.h Source/MainComponent.cpp` reviewed line-by-line against the 8 documented edits (5.2-5.8) above; confirm `paint`, `resized`, and `buildSeededSequence` are byte-identical to the `playback-transport-clock` baseline, and no stray include/member/method was introduced. **Confirmed**: full `git diff` reviewed line-by-line — matches exactly the 8 documented edits, no stray include/member/method; `paint`, `resized`, `buildSeededSequence` confirmed byte-identical to baseline (diff hunks touch only the anonymous namespace, ctor init-list/body, `prepareToPlay`, `getNextAudioBlock`, `releaseResources`, and the dtor).
- [x] 5.10 No `.jucer` FILE-list edit is needed for `Source/MainComponent.h/.cpp` themselves (pre-existing, already-registered files whose content changed). Confirm neither `.jucer`'s `<FILE name="MainComponent...">` entry changed. **Confirmed**: `git diff -- Berlin.jucer Tests/BerlinTests.jucer | grep -i MainComponent` returned zero lines — neither `.jucer`'s `MainComponent` entry changed (the only `.jucer` diffs in the whole working tree are Phases 2-4's additive `midi` group entries).
- [x] 5.11 Build both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64): confirm 0 errors. Run `BerlinTests.exe --category=Berlin`: confirm exit 0, same suite count as end of Phase 4 (`MainComponent` is never compiled into the test project). **Confirmed**: `Berlin.sln` (Debug|x64) built clean, 0 errors, only the 2 pre-existing benign `C4100` warnings in `Source/Main.cpp`. `Tests/BerlinTests.sln` (Debug|x64) built clean, 0 errors, 0 warnings. `BerlinTests.exe --category=Berlin` exit 0, "All tests completed successfully", 80 total `beginTest` blocks — identical to Phase 4's end count, confirming `MainComponent` was not compiled into the test project and no regression occurred.

## Phase 6: Final Gate

- [x] 6.1 `Projucer.exe --resave Berlin.jucer`: confirm exit 0. **Confirmed**: exit 0, "Finished saving: Visual Studio 2026"; idempotent (no further diff from this final resave beyond what Phases 2-4 already produced).
- [x] 6.2 `Projucer.exe --resave Tests/BerlinTests.jucer`: confirm exit 0. **Confirmed**: exit 0, "Finished saving: Visual Studio 2026"; idempotent.
- [x] 6.3 Sync-check: the `Source/(core|generation|playback|midi)/*` `<FILE>` entry sets in both `.jucer` files match exactly modulo the `../` prefix, EXCEPT `MidiEventTranslator.{h,cpp}` and `MidiOutputSink.{h,cpp}`, which by design appear ONLY in `Berlin.jucer` — confirm this asymmetry is exactly those four files and nothing more. **Confirmed**: grepped both files' `Source/(core|generation|playback|midi)/*` `file=` entries — `Berlin.jucer` lists 21 files, `Tests/BerlinTests.jucer` lists 17, and the 4-file difference is exactly `MidiEventTranslator.h`, `MidiEventTranslator.cpp`, `MidiOutputSink.h`, `MidiOutputSink.cpp` — nothing more.
- [x] 6.4 **`<MODULES>` gate (defect, not warning, if violated) — the hard gate for this change**: diff `Tests/BerlinTests.jucer`'s `<MODULES>` block before vs. after this whole change and confirm it is byte-identical — `juce_core` only, no addition. Separately confirm `Berlin.jucer`'s `<MODULES>` block is ALSO byte-identical (it already contained `juce_audio_devices` before this change per the proposal's Dependencies section — no module addition is expected in either file for this change). **Confirmed**: `git diff 5836916 HEAD -- Berlin.jucer` and `-- Tests/BerlinTests.jucer` (baseline = `5836916`, "docs: add midi output tasks", the last commit before this apply session) piped through `grep -E "^[+-]\s*<MODULE "` returned zero lines for both files. `Tests/BerlinTests.jucer`'s `<MODULES>` block still reads `juce_core` only (confirmed by direct read).
- [x] 6.5 Build both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64): confirm 0 errors/0 warnings beyond any pre-existing benign warnings already noted in the archived `playback-transport-clock` tasks.md. **Confirmed**: `MSBuild.exe Builds/VisualStudio2026/Berlin.sln -t:Rebuild -p:Configuration=Debug -p:Platform=x64` exit 0, 0 errors, only the same 2 pre-existing `C4100` "'commandLine': parámetro sin referencia" warnings in `Source/Main.cpp` (lines 24/46) already noted in `playback-transport-clock`'s Phase 6.9/7.5. `MSBuild.exe Tests/Builds/VisualStudio2026/BerlinTests.sln -t:Rebuild -p:Configuration=Debug -p:Platform=x64` exit 0, 0 errors, 0 warnings of any kind.
- [x] 6.6 Run `BerlinTests.exe --category=Berlin`: confirm exit 0, previous suites plus `SequencePlayerStop` and `MidiMessageBytes` all present and green. **Confirmed**: exit 0, "All tests completed successfully". 15 suites present and green: Smoke, Step, Sequence, Scale, DeterministicRandom, PitchGenerator, RhythmGenerator, Reproducibility, StepEvent, StepEventBuffer, Transport, SequencePlayer, PlaybackTiming, SequencePlayerStop, MidiMessageBytes — 80 total `beginTest` blocks (67 from `playback-transport-clock`'s final count + 6 new `SequencePlayerStop` + 7 new `MidiMessageBytes`).
- [x] 6.7 `git diff --stat` for `Source/core/`, `Source/generation/`, `Source/playback/Transport.*`, `Source/playback/StepEvent.h`, `Source/playback/StepEventBuffer.h`, `Source/Main.cpp`: confirm empty — untouched per design's File Changes table (only `SequencePlayer.h/.cpp` in `Source/playback/` is touched, additively). **Confirmed**: `git diff --stat -- Source/core/ Source/generation/ Source/playback/Transport.h Source/playback/Transport.cpp Source/playback/StepEvent.h Source/playback/StepEventBuffer.h Source/Main.cpp` produced no output — all confirmed untouched.
- [x] 6.8 `git diff --stat` overall: confirm only the file set from design's File Changes table appears (`Source/midi/*`, `Source/playback/SequencePlayer.{h,cpp}`, `Source/MainComponent.h/.cpp`, `Tests/Source/{MidiMessageBytesTests,SequencePlayerStopTests}.cpp`, `Berlin.jucer`, `Tests/BerlinTests.jucer`, regenerated `Builds/`/`JuceLibraryCode/`/`Tests/Builds/`/`Tests/JuceLibraryCode/`, plus `openspec/changes/midi-output-routing/*` planning artifacts) — no stray/unplanned files. **Confirmed**: `git status --porcelain` plus `git ls-files --others --exclude-standard` (this apply session made working-tree changes, not new commits) shows exactly: modified `Berlin.jucer`, `Tests/BerlinTests.jucer`, `Source/MainComponent.{h,cpp}`, `Source/playback/SequencePlayer.{h,cpp}`, `openspec/changes/midi-output-routing/tasks.md`, plus regenerated `Builds/VisualStudio2026/Berlin_App.vcxproj{,.filters}` and `Tests/Builds/VisualStudio2026/BerlinTests_ConsoleApp.vcxproj{,.filters}`; new untracked `Source/midi/{MidiMessageBytes.h,MidiEventTranslator.{h,cpp},MidiOutputSink.{h,cpp}}`, `Tests/Source/{MidiMessageBytesTests,SequencePlayerStopTests}.cpp` — exactly design's File Changes table, no stray/unplanned file.
- [x] 6.9 **Review-only gate**: inspect `getNextAudioBlock`'s full call path (`player.process`, `midiTranslator.translate`, `midiSink.dispatch`) line by line and confirm no heap allocation, no `juce::String`/`Logger` call, no `Time::getMillisecondCounter*` read, and exactly one lock acquisition — the documented `sendBlockOfMessages` internal lock — anywhere in it. **Confirmed** (read `MainComponent::getNextAudioBlock`, `MidiEventTranslator::translate`, `MidiOutputSink::dispatch` line by line): `clearActiveBufferRegion()` (JUCE built-in) + `player.process` (unchanged, JUCE-free) + `translate` (`destination.clear()`, per-event `addEvent` into pre-reserved storage, no `ensureSize`) + `dispatch` (guarded `sendBlockOfMessages` call, unconditional `elapsedSamples +=`). Grepped all three files for `new |delete |juce::String|juce::Logger|getMillisecondCounter\(\)|CriticalSection|std::mutex` — the only hits are `juce::String`/`getOpenDeviceName()` (never called from the callback) and `getMillisecondCounterHiRes()` inside `prepare()` (called once per device start from `prepareToPlay`, never per-block). No `new`/`delete`, no lock outside `sendBlockOfMessages`'s own internal `CriticalSection`.
- [x] 6.10 **Review-only gate**: confirm `ensureSize` appears only in `prepareToPlay` (never inside `translate` or `dispatch`), and confirm `bufferToFill.startSample` is not added to any MIDI timestamp anywhere in the dispatch path. **Confirmed**: `grep -rn "ensureSize" Source/midi Source/MainComponent.cpp` shows the only real call is `midiBlock.ensureSize (kMidiBufferBytes);` in `prepareToPlay` (the other two hits are doc-comment prose in `MidiEventTranslator.h` stating `translate` never calls it). `grep -rn "startSample" Source/midi Source/MainComponent.cpp` shows the only hit is doc-comment prose in `MidiEventTranslator.h`; zero occurrences as actual code anywhere in the dispatch path.
- [x] 6.11 **Review-only gate**: confirm the audio callback (`getNextAudioBlock`) contains no device-open or device-close call — only the null/closed check inside `dispatch` gates MIDI output (`midi-device-selection`'s "Device Lifecycle Confined to the Message Thread" requirement). **Confirmed**: `grep -n "openFirstAvailableDevice\|closeDevice" Source/MainComponent.cpp` shows exactly two call sites — `openFirstAvailableDevice()` in the constructor body (before `setAudioChannels`) and `closeDevice()` in the destructor (after `shutdownAudio()`) — neither inside `getNextAudioBlock`.
- [x] 6.12 **MANUAL, NOT AUTOMATABLE — human step, not an `sdd-apply` step**: with a virtual MIDI port (loopMIDI or equivalent) selected as the auto-opened device and a MIDI monitor attached, launch `Berlin.exe` and capture: note-on/note-off pairs on the fixed channel at fixed velocity 100, inter-note intervals matching 120 BPM / 16th notes (125 ms), across at least one full 16-step loop wrap; then quit the app mid-note and confirm the monitor shows the tracked note-off followed by a CC 123 All Notes Off on the same channel, with no stuck note; separately, with zero MIDI devices available (or the virtual port disabled), confirm the app launches, runs, and exits cleanly with no crash, assert, or hang. This is the `midi-output-dispatch`/`midi-device-selection` specs' manual-gate requirement and the design's stated coverage gap — it cannot be reached by the `juce_core`-only headless harness. **Confirmed**: user ran the full gate. (1) With loopMIDI selected, the monitor showed note-on/off pairs at the correct ~125 ms spacing across a full loop wrap. (2) Quitting mid-note showed the tracked note-off followed immediately by CC123, nothing left stuck. (3) Zero-device relaunch (all MIDI outputs disabled) started and ran with no crash. **Real-world gotcha found and worth recording**: `openFirstAvailableDevice()` opening literally the first enumerated device is not a hypothetical edge case on stock Windows — `Microsoft GS Wavetable Synth` (a built-in software synth, not a true PnP device) reliably enumerates before any virtual MIDI port and is *not* reliably removable from enumeration via Device Manager's Disable, even after disabling its Sound-controllers entry. First attempt at this gate produced total silence in the monitor with no error, because MIDI was flowing correctly to GS Wavetable Synth instead (confirmed audibly — the user heard the sequence playing through speakers, proving the dispatch chain itself was already correct). Diagnosed via a temporary `#if JUCE_DEBUG`-gated device-list-and-opened-device log added to `MainComponent`'s constructor, and a temporary `MidiOutputSink::openDevicePreferring(nameContains)` debug-only override to force loopMIDI selection for this test session. **Both temporary additions were fully reverted before this task was marked done** — `MainComponent.cpp` and `MidiOutputSink.h/.cpp` are back to byte-identical with the pre-diagnostic apply output (confirmed via `git diff --stat` showing no residual diff beyond the original Phase 5 changes), both solutions rebuilt clean afterward, full suite re-run green. No production code — including the locked "auto-open first available device, no fallback preference" decision — was changed by this diagnosis; only test-environment friction was resolved.

### Full Suite Confirmation (fill in at execution time)

`BerlinTests.exe --category=Berlin` exit 0, all prior suites plus `SequencePlayerStop` and `MidiMessageBytes` green — **confirmed** (15 suites, 80 `beginTest` blocks total, see 6.6; re-confirmed green after reverting the temporary diagnostic used for 6.12). `Berlin.jucer` GUI app rebuilt clean, 0 errors — **confirmed** (see 6.5; re-confirmed clean after the revert). Tasks 6.1-6.12 all gates green, including the manual smoke test — human-confirmed. Change is ready for `sdd-verify`.

## Deviations from design.md

- **`clearAllPendingMessages()` availability (design.md Open Question, Decision 4)**: design.md flagged this as unverified against a local JUCE checkout at design time. Verified against the pinned JUCE checkout at `H:\Proyectos\Juce\Projucer\modules\juce_audio_devices\midi_io\juce_MidiDevices.h` line 428 during Phase 4 apply — **the method exists exactly as assumed**. No fallback substitution (`stopBackgroundThread()` before `sendMessageNow`) was needed; `closeDevice()` implements the original four-step order from design.md unchanged, with an inline code comment (not just this note) explaining why `clearAllPendingMessages()` must run before `sendMessageNow(CC123)`.
- No other deviation from design.md was required. `kMidiChannel = 1` and `kMidiBufferBytes = 1024` (design's proposed values, per its Open Questions) were used as-is; no user input was available to override them during automated apply.
