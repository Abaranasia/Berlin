# Tasks: MIDI Export

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~450-650 authored (excl. generated `Builds/`/`JuceLibraryCode/`/`Tests/Builds/`/`Tests/JuceLibraryCode/`) |
| 400-line budget risk | Medium |
| Chained PRs recommended | Yes |
| Suggested split | PR1 MidiExportTimeline (JUCE-free, unit-tested) -> PR2 MidiFileWriter (manual-gate) -> PR3 MainComponent wiring + final gate |
| Delivery strategy | ask-on-risk |
| Chain strategy | feature-branch-chain |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: Medium — no single unit is expected to individually clear 400 authored lines, but `MidiExportTimeline` (algorithm + full scenario-table test suite) is dense enough on its own to justify a standalone PR, and the open equal-tick-ordering question (Decision 7) plus the duplicate end-of-track question both land inside PR2/PR3's manual-gate surface, raising reviewer attention cost there.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | `Source/export/MidiExportTimeline.h/.cpp` (JUCE-free tick-domain transform, fully unit-tested) | PR 1 (base: `main`) | `BerlinTests.exe --category=Berlin --name=MidiExportTimeline` | Console run, check exit code | Delete `Source/export/MidiExportTimeline.h/.cpp` + `Tests/Source/MidiExportTimelineTests.cpp`, revert the new `export` group `<FILE>` entries in both `.jucer` files |
| 2 | `Source/export/MidiFileWriter.h/.cpp` (JUCE-aware SMF writer, manual-gate-only) | PR 2 (base: PR1 branch) | None automatable — reviewer inspection only | N/A (needs `juce_audio_basics`, absent from the test project by design) | Delete both files, revert `Berlin.jucer`'s `export`-group `<FILE>` entries (no `Tests/BerlinTests.jucer` entry exists to revert) |
| 3 | `MainComponent` wiring + Final Gate | PR 3 (base: PR2 branch) | `BerlinTests.exe --category=Berlin` (full) | Console run + manual DAW/MIDI-reader import of `Documents/Berlin/berlin-export.mid` (non-automatable gate) | Revert `Source/MainComponent.h/.cpp` to the archived `midi-output-routing` state (byte-verifiable) |

## Phase 1: MidiExportTimeline (JUCE-free tick-domain transform — TDD'd first, no JUCE type touched)

- [x] 1.1 RED: create `Tests/Source/MidiExportTimelineTests.cpp` covering every spec scenario from `midi-export-timeline`: exact-integer tick derivation (`ticksPerStep == 240` @ `stepsPerBeat = 4`, every step boundary `n * ticksPerStep`); no drift across `repeats > 1`; consecutive active steps share a boundary tick with note-off before note-on; full event list non-decreasing in tick; terminal note-off at `endTick` when the last step is active, none when inactive, single-step all-active + `repeats = 1` yields exactly one on/off pair; repeats 1/2/4 produce contiguous non-overlapping passes with no seam gap/duplicate; `repeats` of `0`/`-1` -> `invalidRepeatCount`, `out.events` left empty; `stepsPerBeat` of `0`/`-1`/`7` -> `invalidStepsPerBeat` (7 does not divide 960, rejected not rounded); every note-on velocity is `kNoteVelocity` (via the injected constant path, or a fixed literal matching it); inactive step emits nothing; empty `Sequence()` and all-inactive sequence -> `ok`, no events, correct `endTick`. Include `static_assert (std::is_aggregate_v<MidiExportEvent>)` and `static_assert (kTicksPerQuarterNote % 4 == 0)`. Hand-built `Sequence` per case (same convention as `SequencePlayerTests.cpp`). Register `<FILE>` in `Tests/BerlinTests.jucer`'s `Source` group only. `--resave`, build: confirm compile-time failure (missing `export/MidiExportTimeline.h` include / undefined `buildMidiExportTimeline`).
- [x] 1.2 GREEN: create `Source/export/MidiExportTimeline.h` exactly per design's interface: `kTicksPerQuarterNote = 960`, aggregate `MidiExportEvent` (+ non-member `operator==`), `MidiExportTimeline` struct, `MidiExportStatus` enum, `buildMidiExportTimeline` declaration. JUCE-free — `<vector>` + `core/Sequence.h` only, no other include. Implement the algorithm out-of-line in `MidiExportTimeline.cpp` exactly per design's contract: reject `repeats < 1` and `stepsPerBeat` that doesn't evenly divide 960 (never clamp/round); absolute `tick = k * ticksPerStep`, note-off pushed first at a shared tick, terminal note-off at `out.endTick`; `out` cleared first and left empty on any non-ok status; never re-sort.
- [x] 1.3 Build + regression: rebuild `Tests/BerlinTests.sln`, run `BerlinTests.exe --category=Berlin`: confirm exit 0, previous suite count plus the new `MidiExportTimeline` suite green. Grep `Source/export/MidiExportTimeline.h/.cpp` for the token `juce` (case-insensitive): confirm zero hits — the structural JUCE-free guarantee.
- [x] 1.4 Full 6-step registration checklist — new `<GROUP name="export">` in both projects: (1) add the group to `Berlin.jucer` with `MidiExportTimeline.h/.cpp` `<FILE>` entries. (2) add the mirrored group to `Tests/BerlinTests.jucer` with the same two files at `../Source/export/...` (this file pair only — `MidiFileWriter` must never appear here). (3)-(4) `Projucer.exe --resave` both, exit 0. (5) sync-check: confirm no `<MODULE>` line changed in either file. (6) build both `Berlin.sln` and `Tests/BerlinTests.sln`.
- [x] 1.5 Equivalence test: add a case to `MidiExportTimelineTests.cpp` (or a follow-up test in the same file) driving `SequencePlayer::process` + `flushPendingNoteOff` at a sample rate with integral `samplesPerStep` over one pass, and compare `(order, note, noteOn)` against `buildMidiExportTimeline`'s output for the same `Sequence` — the one automated defence against "exported pattern differs from what you hear".

## Phase 2: MidiFileWriter (JUCE-aware SMF writer — manual-gate-only, no automated test possible)

- [x] 2.1 No RED/GREEN split possible (`juce_audio_basics`, same constraint as Phases 3-4 of `midi-output-routing`). Write `Source/export/MidiFileWriter.h/.cpp` directly per design's exact interface: ctor `MidiFileWriter (int outputChannel, int velocity) noexcept` (both clamped, Decision 8); `buildFile` (`setTicksPerQuarterNote`, tempo meta-event via `MidiMessage::tempoMetaEvent (llround (60'000'000.0 / bpm))`, `timeSignatureMetaEvent (4, 4)`, notes from `timeline.events` 1:1, explicit `endOfTrack` at `endTick`); `writeTo (timeline, bpm, OutputStream&)`; `writeToFile` per Decision 4's exact failure table (`pathUnavailable`/`writeFailed`/`ok`), atomic via `juce::TemporaryFile`, creating the parent directory if needed, never throwing/asserting. Note-off emitted as `juce::MidiMessage::noteOff (channel, note, (juce::uint8) 0)` — no include of `midi/MidiMessageBytes.h` (tier boundary, Decision 8).
- [x] 2.2 **Explicit open-item task — verify `juce::MidiMessageSequence::addEvent`'s equal-tick ordering (Decision 7, highest-severity unknown)**: write a small standalone diagnostic (or inspect the pinned JUCE checkout's `juce_MidiMessageSequence.cpp` `addEvent` implementation directly, same technique as `midi-output-routing`'s verified `clearAllPendingMessages` check) confirming whether `addEvent` preserves insertion order for two events at an identical timestamp. Record the finding in the apply/verify report. If insertion order IS preserved: proceed with `buildFile` calling `addEvent` in the timeline's emitted order and never calling `sort()`/`updateMatchedPairs()` (as coded in 2.1). If it is NOT preserved (reorders by event type or is otherwise unstable): fall back to emitting note-offs at `tick - 1` inside `buildMidiExportTimeline` (Phase 1) instead, and record the substitution explicitly in the verify report — do not silently assume either outcome.
- [x] 2.3 Review-only gate: confirm `MidiFileWriter.h/.cpp` contains no musical branch (all note data comes from the already-built `timeline`), no `sort()`/`updateMatchedPairs()` call anywhere, and does not include `midi/MidiEventTranslator.h` or `midi/MidiMessageBytes.h`.
- [x] 2.4 Registration (adapted 6-step checklist — `Berlin.jucer` only, no `Tests/BerlinTests.jucer` counterpart by design): (1) add `MidiFileWriter.h/.cpp` `<FILE>` entries to `Berlin.jucer`'s `export` group. (2) **N/A** — confirm neither file appears in `Tests/BerlinTests.jucer`. (3)-(4) `Projucer.exe --resave` both, exit 0 (test-project resave is a no-op for this pair). (5) sync-check: no `<MODULE>` change in either file; confirm the `export` group's file-set difference between the two projects is exactly `MidiFileWriter.h/.cpp`. (6) build `Berlin.sln` (this file only compiles into the GUI app).
- [x] 2.5 Build `Berlin.sln`: confirm 0 errors. Run `BerlinTests.exe --category=Berlin` as a regression sanity check only: confirm exit 0, same suite count as end of Phase 1.

## Phase 3: MainComponent Wiring + Final Gate

- [x] 3.1 Pre-check: `git diff --stat -- Source/MainComponent.h Source/MainComponent.cpp` against the archived `midi-output-routing` baseline: confirm empty.
- [x] 3.2 `MainComponent.h` — add exactly: `#include "export/MidiExportTimeline.h"`, `#include "export/MidiFileWriter.h"`; new `const berlin::Sequence sequence;` member declared **before** `player` (Decision 2 — load-bearing order); new `static juce::File resolveExportFile();` private static helper, commented as a **temporary hook, deleted in Phase 7**. `paint`/`resized` stay absent from the diff.
- [x] 3.3 `MainComponent.cpp` anonymous namespace — add `constexpr int kExportRepeats = 4;` and `const char* kExportFileName = "berlin-export.mid";` alongside the existing constants.
- [x] 3.4 `MainComponent.cpp` ctor init list — change to `sequence (buildSeededSequence()), player (sequence, berlin::Transport (kBpm, kStepsPerBeat)), midiTranslator (kMidiChannel), midiSink (kMidiChannel)`; `buildSeededSequence()` still called exactly once, no accessor added to `SequencePlayer`.
- [x] 3.5 `resolveExportFile()` implementation — `juce::File::getSpecialLocation (juce::File::userDocumentsDirectory).getChildFile ("Berlin").getChildFile (kExportFileName)`.
- [x] 3.6 `MainComponent.cpp` ctor body — pure insertion after `midiSink.openFirstAvailableDevice();` and before the `RuntimePermissions`/`setAudioChannels` block: `buildMidiExportTimeline (sequence, kStepsPerBeat, kExportRepeats, timeline)`; on `ok`, construct `MidiFileWriter (kMidiChannel, berlin::MidiEventTranslator::kNoteVelocity)` and call `.writeToFile (timeline, kBpm, resolveExportFile())`; log any non-`ok` `MidiExportStatus` or `MidiFileWriteResult` via `juce::Logger::writeToLog` (return value not silently discarded); startup continues regardless (Decision 4) — no `jassertfalse`, no throw, no dialog.
- [x] 3.7 Verification gate — `git diff -- Source/MainComponent.h Source/MainComponent.cpp` reviewed line-by-line against 3.2-3.6; confirm `paint`, `resized`, `prepareToPlay`, `getNextAudioBlock`, `releaseResources`, and the destructor are byte-identical to the `midi-output-routing` baseline.
- [x] 3.8 No `.jucer` FILE-list edit needed for `Source/MainComponent.h/.cpp` themselves. Confirm neither `.jucer`'s `MainComponent` `<FILE>` entry changed.
- [x] 3.9 Build both `Berlin.sln` and `Tests/BerlinTests.sln` (Debug|x64): confirm 0 errors. Run `BerlinTests.exe --category=Berlin`: confirm exit 0, same suite count as end of Phase 1 (`MainComponent` is never compiled into the test project).
- [x] 3.10 `Projucer.exe --resave Berlin.jucer` and `--resave Tests/BerlinTests.jucer`: confirm both exit 0, idempotent (no further diff beyond Phases 1-2's registrations).
- [x] 3.11 **`<MODULES>` gate (defect, not warning)**: diff both `.jucer` files' `<MODULES>` blocks before vs. after this whole change: confirm byte-identical — `Tests/BerlinTests.jucer` stays `juce_core`-only, `Berlin.jucer` gains no new module (`juce_audio_basics` already linked).
- [x] 3.12 `git diff --stat` for `Source/core/`, `Source/generation/`, `Source/playback/`, `Source/midi/`, `Source/Main.cpp`: confirm empty — untouched per design's File Changes table.
- [x] 3.13 Update `openspec/specs/unit-test-harness/spec.md` per the design's delta: generalize the "shared JUCE-free tiers" and "not duplicated" language from `Source/core/*`/`Source/generation/*` to all JUCE-free source tiers (now including the `export` tier's timeline half), and note the split-tier scenario (unit-tested half vs. manual-gate-only half).
- [x] 3.14 **Explicit open-item task — duplicate end-of-track check**: hex-inspect the track chunk tail of the exported `Documents/Berlin/berlin-export.mid` and confirm it contains **exactly one** `FF 2F 00`. If two are found (JUCE's `writeTrack` did not skip its own end-of-track despite the explicit one written in `buildFile`), drop the explicit `endOfTrack()` call from `MidiFileWriter::buildFile` and re-verify a single `FF 2F 00`; record whichever outcome in the verify report.
- [x] 3.15 Review-only gate: confirm the note-off status byte emitted by `MidiFileWriter` is `0x80 | (channel - 1)` — a real note-off, not a velocity-0 note-on — via hex inspection of a written note-off event.
- [ ] 3.16 **MANUAL, NOT AUTOMATABLE — human step, not an `sdd-apply` step**: launch `Berlin.exe`, then open `Documents/Berlin/berlin-export.mid` in a DAW or MIDI-aware reader/importer and confirm: it opens without error or repair prompt; 120 BPM; 4/4; exactly 4 bars; every note velocity 100 on channel 1; note lengths exactly one 16th; pitches match the seeded pattern; no note extends past the final bar; every note-on has a matching note-off and the track's last note event is a note-off (no stuck note). Additionally: export twice with different `kExportRepeats` values and confirm the second import shows no trailing bytes from the first (atomic overwrite); and confirm a run with `Documents/Berlin` made read-only launches, plays, and exits cleanly with a logged failure and no zero-byte/truncated file left behind. Capture a screenshot or event dump. Leave this task unchecked, awaiting human verification — same as `midi-output-routing` task 6.12.

### Full Suite Confirmation (filled in at execution time)

`BerlinTests.exe --category=Berlin` exit 0, 94 `Starting tests in` lines (14 suites + `MidiExportTimeline`'s 14 sub-tests added to the prior 80), all green, no regression. `Berlin.jucer` GUI app rebuilt clean (`MSBuild.exe ... Berlin.sln -t:Build`), 0 errors, only the 2 pre-existing benign `C4100` warnings in `Source/Main.cpp`. `Tests/BerlinTests.sln` rebuilt clean, 0 errors, 0 warnings. Tasks 3.1-3.15 all gates green, including an actual `Berlin.exe` launch (8s, timeout-killed) that produced a real `Documents/Berlin/berlin-export.mid` (349 bytes), hex-verified: exactly one `FF 2F 00` at the file's tail (offset 346), tempo meta-event `07 A1 20` = 500000us = 120 BPM, time-signature meta-event `04 02 01 60` = 4/4, and note-off status bytes are `0x80` (channel 1) — a real note-off, not velocity-0 note-on. Task 3.16 (full manual DAW import + atomic-overwrite + read-only-directory gates) is left unchecked, awaiting human confirmation. Change is ready for `sdd-verify` only after 3.16 is confirmed.
